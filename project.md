# The Family Downloader — Project Plan

A self-hosted pipeline that turns a tap on an iPad into the right movie, at the right quality, sitting in qBittorrent — with every torrenting decision made and hidden in the backend. The family browses TMDB-style, taps "Add to Plex," and never sees a torrent name.

This file is the persistent source of truth across stages and chat sessions. Each stage should be worked in its own chat; **read this whole file at the start of that chat**, and **append to the Decision Log below at the end of it** before closing out. The plan sections (target environment, principles, stages) should only change when a decision genuinely changes the design — not on every session.

Full staged proposal with design rationale: https://claude.ai/code/artifact/bcbb1dfc-40da-4c78-a94e-fe74194f7dd7

---

## Status

- **Current stage:** Stages 0-8 (movies) complete and deployed. Stage 9 (TV discovery & episode resolution) built and unit-tested, not yet pushed/deployed to the NAS. Stages 10-14 (episode-aware matching, post-download file organization/Plex layout, subscriptions/scheduling, whole-season/complete-series bulk acquisition, TV frontend) designed but not yet built — see the Stages section and the 2026-09-05/2026-09-06 decision-log entries below.
- **Deployed:** yes — running on the real TrueNAS SCALE box (SmithFamilyNAS) as a Custom App named `movie-downloader`, frontend published at `http://192.168.0.133:8095/`, at commit `49b860c` (Stage 8) as of this file's last update. The user pulled it live via the Settings panel's "Check for updates" button; confirmed genuinely live (not just reported) with direct `curl`s from this workstation immediately after — `GET /api/health` returned `{"status":"ok","qbittorrent":true}` and `GET /api/admin/jobs` correctly surfaced an old "no qualifying results" row as raw JSON. Self-update: the Settings panel's "Check for updates" button runs a genuine `git pull` against a live, read-only GitHub deploy key. Stage 9's code isn't on the NAS yet — nothing in it changes a running route, so there's no user-facing gap yet, but it still needs a commit/push and a "Check for updates" click before the deployed clone matches the working tree.
- **Last updated:** 2026-09-06.

---

## Target environment

- **Host:** TrueNAS SCALE, deployed as a Custom App (Docker Compose–based). Custom App YAML supports both `image:` (pull) and `build:`/`dockerfile_inline` (build on the NAS from source).
- **Torrent client:** qBittorrent (v4.x/v5.x), WebUI enabled, already running as its own existing TrueNAS app — not deployed by this project.
- **Search mechanism:** qBittorrent's internal Search Plugins, driven asynchronously via `/api/v2/search/start`, `/status`, `/results`, `/delete`, using the `qbittorrent-api` Python library's `search.start()` job object.
- **Metadata/browse source:** TMDB API — search, discover-by-watch-provider, popular/trending, alternate titles.
- **Client device:** iPad, Safari, touch-first, "Add to Home Screen" standalone mode.
- **Repo:** git repo already initialized at this project root, remote `origin/main` configured.

---

## Confirmed architecture (not open for re-litigation)

- **Three logical components, one deployable app:**
  1. **Frontend container** — a single static HTML file (no framework, no build step) served by nginx, which reverse-proxies `/api/*` to the backend. Only component with a published port.
  2. **Backend container** — FastAPI automation engine. No published port; reachable only from the frontend container over the internal Compose network (`backend:8000`).
  3. **qBittorrent** — existing separate app, reached by the backend over the network.
- **Same-origin only.** All client traffic goes through the frontend's single exposed port — no CORS, no two-port setup.
- **Filter on structured API fields**, not filename regex, wherever possible (`fileSize`, `nbSeeders`). Only resolution, language, source, codec and container need filename token parsing — always whole-token, normalized matching, never substring.
- **Deploy is git-pull-based, not live-reload-on-edit.** A working copy (wherever edits happen) is fully decoupled from a deployed copy bind-mounted into each container. The deployed copy only changes via `git pull`. Backend picks it up via `uvicorn --reload`; frontend picks it up on next request since there's no build step. Rollback is `git checkout`/`git revert` on the deployed copy.
- **Self-update endpoint** (`POST /api/admin/deploy`) runs exactly `git pull`, hardcoded, no path/branch/command ever accepted as input. Gated by a plain, visible "Check for updates" button inside the Settings panel — not real auth, an accepted risk (see Stage 6; originally planned as a hidden long-press, changed to a visible button mid-Stage-6 per explicit user direction, prioritizing simplicity over obscurity — the accepted-risk reasoning is unchanged either way).
- **Explicitly out of bounds:** mounting the Docker socket into the backend. Self-update means "pull my own repo," nothing more privileged.
- **Requests are serialized** through a single `asyncio.Lock` — no distributed job queue, sufficient at household scale.
- **Job/request state persists in SQLite** on a volume, surviving container restarts.
- **No family profiles in the UI.** One shared Settings panel instead of per-user identity; the audit trail tracks what was picked, not who asked.

---

## Design principles carried through every stage

- **Pipeline as a library.** Search → filter → score → select → add is plain Python with no FastAPI dependency. The API is a thin wrapper, which is what makes it CLI-testable before any web layer exists.
- **One normalizer, many uses.** The same token-normalization utility matches resolution, language, source, codec, container, and title/year relevance — not several ad hoc string checks.
- **Two passes, never one.** "Is this actually the right movie" (relevance) and "which release of it is best" (quality) are different problems, solved differently — the first stays conservative and rule-based, the second is where the rich scoring lives.
- **Fail safe, not best guess.** When a match is ambiguous, the system reports "no qualifying results" rather than confidently downloading something close-but-wrong.
- **Hidden, never unrecoverable.** The family never sees torrent names or seeder counts — but every job keeps the candidate it picked, the search variant that found it, and its score breakdown, so a wrong download can be audited after the fact.
- **Secrets never reach the browser.** TMDB key, Plex token, git deploy key — all server-side only. The frontend hotlinks TMDB's public image CDN directly; every data call goes through the backend proxy.
- **Deploy is one fixed verb.** `git pull`, hardcoded. No Docker socket, ever.
- **Same origin, one door.** nginx is the only published port.

---

## Stages

Sequencing note: the original spine (engine → API → frontend → containerize → deploy → family → hardening) held, with three deliberate changes — title resolution split out as its own stage ahead of the qBittorrent pipeline; persistence moved earlier into the API stage rather than the family layer, since restart-safe job state is a day-one requirement; and Docker Compose pulled forward into the frontend stage, so TrueNAS packaging becomes mechanical rather than exploratory.

### Stage 0 — Feasibility spike
*Throwaway. No dependencies.*

**Purpose:** Verify qBittorrent's search plugins and TMDB actually behave the way this whole plan assumes, before writing real code.

**Deliverables / done when:**
- A note on which plugins are enabled and return results; how often `nbSeeders` is `-1`/missing and `fileSize` is absent; the real `fileUrl` vs. `descrLink` ratio.
- A TMDB API key created, with a handful of real lookups tried, including one franchise title, to see alternate-title data quality.

**Risks / decisions:** If plugins are unreliable on seeders/size, filter defaults in Stage 2 need to change before they're built.

**Validation:** Whether this plan's assumptions survive contact with real data.

---

### Stage 1 — Media discovery & resolution
*Depends on: Stage 0. No qBittorrent dependency.*

**Purpose:** Everything the iPad UI browses and searches is TMDB, proxied through the backend. Owns the whole browse surface (poster grids, provider rows, search) plus resolving one selected title into a media identity and an ordered list of qBittorrent query variants.

**Deliverables / done when:**
- TMDB client wrapper: search, alternate-titles, discover-by-watch-provider, popular/trending, provider list — called only from the backend, key never reaches the browser.
- In-memory TTL response cache in front of popular/discover/provider-list calls.
- Variant generator: up to 3–4 ranked queries — canonical title, `original_title` if different, title without subtitle, title+year.
- Shared normalization/token-matching utility (reused by Stage 2 for language, source, codec, container, and title relevance).
- CLI: `resolve <tmdb-id>` prints resolved media + variant list.

**Settled:**
- TMDB over OMDB.
- Family disambiguates *which* movie (by browsing/tapping a poster); backend only ever disambiguates *which release* of that already-specific movie.
- In-memory TTL cache, not a persisted cache table — kept as the plan's default; nothing in Stage 0/1 surfaced a reason to persist it.

**Validation:** Unit tests on the normalization utility. Manual CLI/HTTP runs against real TMDB data — search, a provider's popular list, resolution for a franchise title.

**Stage 1 complete.** Implementation lives in `backend/app/`: `normalize.py` (shared token utility), `cache.py` (generic `ttl_cache` decorator), `tmdb.py` (client wrapper — search, get_movie, alternative_titles uncached; popular/trending/watch_providers/discover_by_provider TTL-cached at 300s), `resolve.py` (`MediaIdentity` + `generate_variants`), `cli.py` (`resolve <tmdb-id>`). 14 unit tests passing (`backend/tests/`). Manually verified live against TMDB: `resolve 693134` (Dune: Part Two) → 3 variants; search, alternative_titles (35 entries), popular, watch_providers, and discover_by_provider all returned data matching the Stage 0 spike's numbers exactly. TMDB key lives in gitignored `backend/.env`, loaded via `python-dotenv`, never referenced by the frontend.

---

### Stage 2 — Match, score & add pipeline
*Depends on: Stage 1, Stage 0. The core engine.*

**Purpose:** Two passes. **Pass one** (relevance gate): is this candidate actually the title Stage 1 resolved? Conservative, rule-based. **Pass two** (quality score): among everything that survives pass one, which release is best — ranked by source, codec, container and size, not just seeders?

**Deliverables / done when:**
- Search executor: `search.start()` per variant, polled via `.status()` against a 45–60s ceiling, results via `.results()`, `.delete()` always in `try/finally`.
- **Pass one:** normalized-title overlap against every Stage 1 title variant, plus year within ±1, plus a resolution *floor* (see below), language allow/blacklist, and a cam/telesync/screener blocklist (`cam`, `hdcam`, `ts`, `telesync`, `screener`, `r5`, etc. — always on, unlike the language lists which default open) — all via the shared token utility, no fuzzy-distance matching. The cam blocklist only catches an *explicitly*-tagged bootleg; a real-world test on a movie 5-6 weeks past theatrical (Spider-Man: Brand New Day, 2026) found the only real risk case was releases with no source tag at all (`4K (Small File)`), which this can't catch since there's nothing to match against — noted as a known gap, not solved here.
- **Pass two:** weighted composite score, resolution (2160p/4K > 1080p > 720p > 480p) ranked above source type (REMUX > BluRay/BDRip > WEB-DL > WEBRip > HDTV), codec (HEVC/x265 > AVC/x264), seeder health (a coarse tier — e.g. 100+/30+/10+ — not raw count), and container (MKV > MP4 > other) — same token utility. Seeder health outranks container, not the reverse: container is a weak, easily-missing signal (a REMUX's container is very often left unstated since MKV is the de facto default) while a large swarm-size gap is a real practical difference — a live cross-check confirmed a well-seeded release with no stated container losing to a barely-seeded one that merely spelled its container out. `nbSeeders` is a real (if bounded) term in the composite, plus a practical viability floor before scoring even starts and a final known-then-raw-count tiebreaker (ahead of size) after — never able to override resolution, source, or codec, since Stage 0 found ~50% of real rows report it as unknown. File size (already bounded by a configured GB range) breaks any remaining near-ties.
  - *Worked example (from your Dune case):* of `dune.2160p.remux.webdl.mkv` (40GB), `dune.2160p.HEVC.remux.mkv` (85GB), and `dune.2160p.h264.webdl.mp4` (34GB), the second wins — best source (remux) + best codec (HEVC) + best container (mkv), with size confirming it. (All three sit at the same resolution tier, so resolution doesn't move this particular example — it only starts to matter once candidates at different resolutions are being compared, which is exactly the fallback case below.)
- **Resolution as a floor, not a fixed gate:** a `MIN_RESOLUTION` setting (default `2160p`, editable in Stage 7) — any candidate at or above that tier passes pass one; resolution is then the highest-weighted tier in pass two, so a 2160p release always outranks a 1080p one when both qualify, but a 1080p release is still accepted once the floor is lowered and nothing higher is available. This is what makes "prefer 4K, fall back to 1080p if nothing at 4K qualifies" work inside a single search pass, with no separate resolution-specific retry loop. A candidate with no recognized resolution token at all still fails the floor regardless of setting — fail-safe, not a guess.
- Fallback loop across Stage 1's variants, stopping at the first that produces any gate-passing candidate.
- Dedup within a result set (normalized name+size or infohash) and against `torrents_info()` already present.
- Category ensure-exists, then add via `fileUrl` — skip `descrLink`-only results.
- CLI: `download <tmdb-id>` runs the full pipeline and prints the winner *and its score breakdown* — this is the audit trail until Stage 8.

**Settled:**
- Free space read from qBittorrent's own `server_state.free_space_on_disk` (via `sync/maindata`) — no extra bind mount into the backend, and now actually wired into the pipeline as a pre-add gate (see below).

**Open decisions:**
- [x] Resolution stays a hard 2160p/4K gate — quality tiering applies to source/codec/container *within* it, not across resolutions (no falling back to 1080p). **This was the biggest open call in the plan — reopened and resolved differently mid-Stage-2**, after the user asked whether 1080p fallback would ever be a frontend setting. Landed on: resolution becomes a *floor* setting (`MIN_RESOLUTION`, default `2160p` — so today's default behavior is unchanged) plus the top-weighted pass-two tier, rather than a fixed pass-one token check. See the "Resolution as a floor" deliverable above and `backend/app/score.py`.
- [x] Quality ranking implemented as a weighted composite score, not literal nested tier-group cascading. `resolution_score*10000 + source_score*1000 + codec_score*100 + seeder_score*10 + container_score*1`; known-then-raw seeder count, then size, as final tiebreakers — see `backend/app/score.py`. Reworked three times within Stage 2: first to fold resolution in as the dominant term; then to fold seeder health in as (at the time) the smallest term; then, after a real-world cross-check caught it mattering too little, swapped so seeder health outranks container instead of the reverse — see the decision log for the concrete case that drove that last change.
- [x] Category scheme: one bucket for everything. Single `"movies"` category (`config.CATEGORY`); no movies/TV split (this pipeline is movies-only).
- [x] Exact source/codec/container tier lists and point values — pinned down in `backend/app/config.py`: resolution 2160p/4K(4) > 1080p(3) > 720p(2) > 480p(1); source REMUX(5) > BluRay/BDRip(4) > WEB-DL(3) > WEBRip(2) > HDTV(1); codec HEVC/x265(2) > AVC/x264(1); container MKV(2) > MP4(1); seeder health 100+(3) > 30+(2) > 10+(1), unknown scored the same as tier 1 (viable, not punished — Stage 0's "unknown != zero" — but not rewarded above a verified swarm either). Each tier matches multiple real-world spelling variants (e.g. `WEB-DL`/`WEBDL`, `H.265`/`x265`), found by testing against Stage 0's live sample. `MIN_SEEDERS` (the pass-two viability floor) raised from 1 to 10 in the same pass — a bare "not completely dead" check didn't protect against a genuinely slow download.

**Validation:** Unit tests for the scorer against fixture result sets, including the Dune example above as a named test case, plus messy-field cases (`nbSeeders` of `-1`/`0`, missing `fileSize`, `descrLink`-only). Manual CLI runs against real qBittorrent, including one deliberately hard-to-match title.

**Stage 2 complete.** New modules in `backend/app/`: `qbt.py` (qBittorrent client wrapper — `search()` runs one `search.start()` job to the 55s ceiling with `.delete()` in `finally`, plus `existing_torrent_hashes()`, `free_space_bytes()` via `sync_maindata()`, `ensure_category()`, `add_torrent()`); `score.py` (plugin/result trust filter, pass-one relevance gate — title-variant phrase match, year ±1 tolerance, resolution floor, language allow/blocklist, cam/telesync/screener blocklist — viability gate, dedup by infohash/name+size, pass-two weighted composite scorer with resolution as the dominant tier and seeder health outranking container); `pipeline.py` (`download()` — the fallback loop across Stage 1's variants, gating the ranked winner against qBittorrent's real free space before adding, with a distinct `"insufficient free space"` status alongside `"added"`/`"no qualifying results"`). CLI gained `download <tmdb-id>`, printing the winner and its full score breakdown (now including `resolution_score` and `seeder_score`) as the audit trail. 68 unit tests passing (`backend/tests/`), including the Dune worked example from this file as a named test case, messy-field fixtures (`nbSeeders` unknown/known-zero, missing `fileSize`, jackett-style localhost `fileUrl`), and the resolution-floor/fallback behavior (floor rejects/admits by tier, ranking prefers the higher resolution when both qualify, falls back to the lower one when it's all that's available). One deviation from the plan text beyond the resolution rework: added a `free_space_bytes()` gate that wasn't explicitly listed under Stage 2's deliverables but was already "Settled" as a data source — wiring it in now (skip an over-large winner, try the next-best candidate, else the next variant) matches "fail safe, not best guess" and meant it didn't sit unused until Stage 3.

Manually verified against the real NAS (qBittorrent v5.2.3 at `192.168.0.133:30024`, confirmed reachable and unauthenticated from this network, matching Stage 0): `download 693134` (Dune: Part Two) found 123 qualifying candidates on the first variant and added `Dune Part Two 2024 2160p BluRay REMUX DV HDR ENG LATINO DDP5.1 H265 MP4-BEN THE MEN` (65.8GB, torlock, `nbSeeders` unknown) — resolution 4/source 5/codec 2/container 1. This was a real add to the household's qBittorrent (confirmed with the user before leaving it running rather than deleting it) — worth remembering for future stages' manual validation runs, which will keep triggering real downloads unless a disposable/obscure title is used instead. `download 895` (Andrei Rublev, 1966 — deliberately hard-to-match, no plausible 2160p release) correctly fell through both of its variants and returned `"no qualifying results"` at the default floor. The resolution-floor rework was then verified read-only (gate/scoring logic run against real search results, without calling `add_torrent`) against that same Andrei Rublev search with the floor temporarily lowered: 33 of 77 raw results qualified at a `720p` floor, top-ranked `Andrei Rublev (1966) Criterion (1080p BluRay x265 HEVC 10bit AAC 1 0 Russian Tigole)` — confirming the floor+tier logic parses real 1080p release names correctly without needing another live add to prove it. No deviations from the plan otherwise. Ready for Stage 3.

---

### Stage 3 — Backend API & persistence
*Depends on: Stage 2, packaged as an importable library.*

**Purpose:** Wrap Stages 1–2 in FastAPI, add the SQLite job store so request state survives restarts, define the API contract the frontend builds against.

**Deliverables / done when:**
- SQLite schema: requests table (id, query text, resolved TMDB id/title, status, timestamps, result-summary fields for the audit trail); a settings table (see Stage 7).
- `POST /api/search` (TMDB candidates), `POST /api/requests` (confirm → enqueue), `GET /api/requests[/…]` (status polling), stub `POST /api/admin/deploy` (real logic in Stage 6).
- `asyncio.Lock`-guarded worker driving one request at a time through: `queued → searching → downloading | no qualifying results | failed → complete`.

**Open decisions:**
- [x] Plain polling (default) over WebSockets/SSE for status updates. Kept the default: `GET /api/requests/{id}` (and `GET /api/requests` for the list view) — no push channel.
- [x] Boot-time recovery sweep — **resolved differently from the plan's literal text.** The plan's draft wording force-failed any row stuck in *either* `searching` or `downloading` on restart. Reopened during this session: `downloading` means a torrent already exists in qBittorrent, independent of this backend — force-failing it on every restart would be a false negative (the download is fine; only this backend's tracking of it was interrupted), violating "hidden, never unrecoverable." Landed on: only `searching` rows (pipeline mid-flight, nothing added yet, that work is genuinely and completely lost) get force-failed to `failed — interrupted, please retry`; `downloading` rows are left untouched and the download watcher just resumes polling them on its next cycle, no special recovery path needed. See `backend/app/db.py`'s `recover_interrupted()`.

**Validation:** API tests with the pipeline mocked, plus one true end-to-end manual run through the API against real qBittorrent + TMDB, including a deliberate backend restart mid-job.

**Stage 3 complete.** New modules in `backend/app/`: `db.py` (`RequestStore` — plain `sqlite3`, no ORM; `requests` table with a JSON `result` blob for the audit trail — variant used, winning candidate, score breakdown, torrent hash; a `settings` table created now as a single seeded-empty-JSON row, schema only, for Stage 7 to define and use); `worker.py` (`Worker` — one `asyncio.Queue`-fed loop processes requests one at a time under an `asyncio.Lock` guarding only the search/score/add phase, driving `queued -> searching -> downloading | no qualifying results | insufficient free space -> failed`; a second, independent loop polls qBittorrent every `DOWNLOAD_POLL_INTERVAL_SECONDS` (10s default) for rows sitting in `downloading` and flips them to `complete` once `progress >= 1` — deliberately *not* holding the pipeline lock, since the actual bittorrent transfer happens inside qBittorrent, outside this backend, and shouldn't block the next queued request); `api.py` (FastAPI app, no CORS — same-origin only, per the confirmed architecture; `POST /api/search` thin-wraps `TMDBClient.search_movie`; `POST /api/requests` resolves the tmdb_id via Stage 1's `resolve()`, persists a `queued` row, and enqueues it; `GET /api/requests` / `GET /api/requests/{id}` for polling; `POST /api/admin/deploy` stubbed at 501, real logic in Stage 6). One deviation beyond the plan's literal deliverables list: **seven** terminal-ish statuses, not Stage 4's originally-stated six (`queued`, `searching`, `downloading`, `complete`, `no qualifying results`, `failed`) — Stage 2 had already added `"insufficient free space"` as a distinct pipeline outcome after that count was written, and it's semantically different from `"no qualifying results"` (found candidates but no room, vs. found nothing), so it's kept as its own status rather than folded into `failed`. Also extended `backend/app/pipeline.py`/`qbt.py`: `DownloadResult` gained `torrent_hash`, captured by diffing `existing_torrent_hashes()` before/after `add_torrent()` (works for both magnet and direct-.torrent-URL winners, unlike parsing `fileUrl` — most real winners are the latter, per Stage 0/2's live sample) — needed so the download watcher knows which torrent to poll; `QBTClient` gained `torrent_info(hash)`. 34 new tests (`test_db.py`, `test_worker.py`, `test_api.py`, plus hash-capture cases added to `test_pipeline.py`), 103 total passing. `requirements.txt` gained `fastapi`, `uvicorn[standard]`, `httpx`; `.gitignore` gained `backend/data/` (the SQLite file's directory).

Manually verified against the real NAS end-to-end through the live API (not just the pipeline), using a disposable test title (Big Buck Bunny, tmdb_id 10378 — a real, legally-torrentable short film with genuine 2160p releases, chosen specifically so this validation doesn't collide with anything the household actually wants, unlike Stage 2's Dune runs) after temporarily lowering `MIN_SIZE_GB` to admit its ~850MB files (reverted immediately after — this exposed and was caught by `test_score.py`'s existing size-floor test, confirming the revert was necessary and complete): (1) `POST /api/requests` → `queued` → `searching`; killed the backend process mid-search and restarted it — the boot recovery sweep correctly marked that request `failed` / `interrupted, please retry`, confirming the resolved open decision above. (2) A second request run to completion added a real torrent (torlock, non-magnet URL) and reached `downloading`; restarting the backend again left it at `downloading`, untouched — confirming the "don't force-fail a real in-progress download" resolution actually holds against a real restart, not just the unit test. (3) **Found and fixed a real bug in the process:** the first live add's `torrent_hash` came back `null` — checking qBittorrent directly confirmed the torrent *had* been added successfully, but `existing_torrent_hashes()` diffed immediately after `add_torrent()` didn't yet show it, since qBittorrent hadn't finished fetching/parsing the remote `.torrent` file from the torlock URL yet (a magnet would index instantly; a direct URL doesn't). Fixed by making `_capture_new_hash()` retry (`HASH_CAPTURE_ATTEMPTS`/`HASH_CAPTURE_INTERVAL_SECONDS`, new `config.py` constants, default 5×1s) instead of checking once — a new unit test (`test_download_retries_hash_capture_until_torrent_is_indexed`) proves the retry mechanism itself, since real qBittorrent indexing delay isn't deterministic enough to re-test live. The disposable test torrent was deleted from qBittorrent afterward (0 seeders, no value in keeping it); Dune from Stage 2 was left untouched. Ready for Stage 4.

---

### Stage 4 — Touch frontend & same-origin proxy
*Depends on: Stage 3's stable API contract. Single static HTML file.*

**Purpose:** A TMDB-style browsing experience, hand-built: home grid of posters, streaming-provider rows, a detail view, and one button — **Add to Plex** — the only torrenting-adjacent thing the family sees. Also proves the same-origin nginx + backend architecture from a real iPad.

**Deliverables / done when:**
- Home grid from `/api/discover`; tap a provider row to browse its popular titles.
- Detail view with full TMDB info; "Add to Plex" button posts the confirmed request (`POST /api/requests`) — no torrent details ever shown.
- Status list using the six states as distinct visuals, not one spinner.
- One HTML file — markup, CSS, JS together, no build tool, no `node_modules`. TMDB poster images hotlinked directly from TMDB's CDN; every data call goes through the backend proxy.
- Local Docker Compose (nginx + backend) proving reverse-proxy + service-name DNS works from a real iPad on the LAN.
- Web app manifest, apple-touch-icon, standalone display mode. No service worker / offline caching — stale cached job status would be actively misleading.

**Settled:**
- No framework, no build step.

**Open decisions:**
- [x] `Cache-Control: no-cache` on the served HTML — resolved: `frontend/nginx.conf` sends `no-cache` on `/` and `/index.html` specifically (forces revalidation every load, cheap at household scale) while manifest/icons get a modest 1-hour cache, since they aren't job-status-sensitive.

**Validation:** Manual touch-testing on a real iPad, in Safari and after "Add to Home Screen," walking browse → provider row → detail → confirm, and all six status states (seed a couple of failure/no-result cases deliberately). No automated UI suite for v1.

**Stage 4 complete**, with one caveat flagged below. New `frontend/` directory: `index.html` (single file — markup/CSS/JS together, no build tool, hash-based router with four views: home, provider browse, search results, movie detail, plus a requests list), `manifest.json`, `apple-touch-icon.png` + `icon-192.png`/`icon-512.png` (generated placeholders — a simple circle-and-play glyph on the app's dark theme; swap for real artwork whenever branding is decided, nothing else depends on their content), `nginx.conf` (reverse-proxies `/api/*` to `backend:8000`, serves the static files, resolves the Cache-Control open decision above), `Dockerfile` (nginx:alpine). New `backend/Dockerfile` (python:3.11-slim, `uvicorn --reload`, matching the confirmed deploy model). New root-level `docker-compose.yml` wiring both — frontend gets the only published port (`8080:80`), backend is `expose`-only, reachable as `backend:8000`, per the confirmed architecture; a named volume backs `backend/data` so the SQLite job store survives container recreation.

One deviation beyond the plan's literal deliverables, needed to make the home grid and provider rows possible at all: the plan's API contract (Stage 3) never actually grew a `/api/discover` route — only `/api/search` and `/api/requests*`. Added to `backend/app/api.py`, all thin pass-throughs of `tmdb.py`'s already-TTL-cached methods (key still never reaches the browser): `GET /api/discover/popular`, `GET /api/discover/trending`, `GET /api/discover/providers`, `GET /api/discover/providers/{id}` (paginated, `page`/`total_pages` in the response so the frontend can drive "Load more"), and `GET /api/movies/{tmdb_id}` for the detail view's full TMDB info (overview, runtime, genres, backdrop). 6 new API tests (`backend/tests/test_api.py`, extending `FakeTMDBClient` with the discover methods), 109 total passing. The home screen's "Browse a service" row uses a curated, hardcoded list of 8 major provider TMDB ids (Netflix, Prime Video, Disney+, Max, Hulu, Apple TV, Paramount+, Peacock) rather than surfacing TMDB's full 100+-provider list — names/logos still come live from `/api/discover/providers`, only which ones get a tap target is fixed in the frontend.

Also a genuine (if minor) status-count correction to the plan's own text: the Stage 4 write-up above still says "all six status states," but Stage 3 already settled on seven (`"insufficient free space"` split out from `"failed"`) — the frontend's `STATUS_META` table implements all seven with distinct pill colors/copy (queued/gray, searching/blue, downloading/purple, complete/green, no qualifying results/amber, insufficient free space/red, failed/dark red); the "six" in this stage's own purpose text is now simply stale, superseded by Stage 3's decision log entry.

**Validation caveat, stated plainly:** no real iPad and no Docker were available in this session — `docker`/`docker-compose`/`nginx` are all absent from this machine, so the Compose stack, the actual nginx reverse-proxy behavior, and genuine Safari/"Add to Home Screen" standalone-mode testing are **unverified**, not just untested. What *was* verified, live, against the real NAS-hosted backend (TMDB + qBittorrent, same instances every prior stage used) and a same-origin proxy standing in for nginx (throwaway `python -m http.server`-based script, not part of the repo): the home grid (trending row + popular grid with working "Load more" pagination) rendering real TMDB data; a provider row tap → provider browse view → its own "Load more"; the search bar (`/api/search`) returning real results; the movie detail view (backdrop, poster, genres, runtime formatting, overview) via the new `/api/movies/{id}` route; and the full "Add to Plex" round trip — tapping the button on a real detail page created a real `POST /api/requests`, the button disabled and showed "Added," an inline status pill appeared and polled every 4s, and — using Andrei Rublev (895, the same known-no-qualifying-match title from Stage 2/3's own validation, chosen specifically so this run couldn't trigger a real download) — the pipeline actually ran end-to-end against real qBittorrent and the pill correctly progressed queued → searching → "No matches found" (amber), matching the requests-list view's rendering of the same row. No torrent name, engine, seeder count, or score ever appeared in any frontend view, confirmed by inspecting the rendered DOM directly, not just the design. The `Add to Plex` flow's real side effects were therefore proven at the API/pipeline layer exactly as thoroughly as prior stages, just not through nginx or a physical iPad — Stage 5's TrueNAS packaging (or a follow-up session with Docker available) should still do a real Compose build and a real device pass before this is fully closed out.

---

### Stage 5 — TrueNAS packaging
*Depends on: Stage 4's proven local Compose setup. Mechanical by design.*

**Purpose:** Port the already-proven local Compose stack to a TrueNAS SCALE Custom App.

**Deliverables / done when:**
- Custom App YAML: frontend (published port) + backend (`build:`, no published port, `backend:8000` DNS); qBittorrent left as-is.
- Volumes: persistent SQLite data volume; deployed-copy bind mount for backend source, distinct from any workstation path.
- `uvicorn --reload` confirmed live against the mount (edit a file directly in it, watch it reload) before git enters the picture.

**Open decisions:**
- [x] Monorepo (default: `frontend/` + `backend/` in one repo) vs. split repos — **stayed monorepo**, which was already the repo's actual shape (confirmed via `git remote -v`: one GitHub repo holding both). One `git clone` on the NAS (`/mnt/SSD/AppData/movie-downloader/repo`) backs both services' `build.context`.
- [x] Verify TrueNAS SCALE's UID/GID handling on bind-mounted storage on the real NAS — **confirmed identical to Docker Desktop, no divergence.** Neither service sets a `user:` directive, so both containers run as root by default; a real live write (the SQLite job store's first `app.db`) landed on the ZFS-backed bind mount as `root:root` despite the host directory being owned by `truenas_admin:truenas_admin` — root inside a container isn't subject to host DAC checks, same as it wasn't inside Docker Desktop's VM. No `ixsystems/container-utils`-style permissions sidecar (the pattern TrueNAS's own qBittorrent/Plex catalog apps use to run as non-root UID 568) was needed for this custom app.

**Validation:** App running on the NAS, frontend reachable from an iPad on the LAN, backend reaching the real qBittorrent app internally, one true end-to-end request against the NAS-hosted stack.

**Stage 5 complete.** New `truenas/custom-app-compose.yaml` in the repo — the actual Custom App compose config, applied to the real NAS via `midclt call app.create` (TrueNAS SCALE's own middleware method for a `custom_app: true` install with a `custom_compose_config_string`, the same mechanism the household's own qBittorrent app already used, confirmed by inspecting its live `app.config` output first and modeling ours on it). Two services, one deployed-copy git clone: `frontend` (nginx, published on host port `8095`, chosen free of the `80`/`443` TrueNAS UI already held) and `backend` (no published port, reachable inside the app's own compose network as `backend:8000` — `nginx.conf`'s `proxy_pass` needed no changes at all). `backend/Dockerfile`'s standing comment ("Stage 5 replaces this with a deployed-copy bind mount") turned out to describe exactly what shipped: both services still `build:` from the Dockerfiles Stage 4 wrote (baking in dependencies/nginx config at image-build time), but each also bind-mounts its live deployed-copy source over what the image baked in — `backend/app:/app/app` (so `uvicorn --reload`, already in the CMD, watches the real deployed copy) and, after a real bug forced a rework (below), the whole `frontend/` directory over `/usr/share/nginx/html`. Persistent SQLite storage is a separate bind mount, `/mnt/SSD/AppData/movie-downloader/data:/app/data`, distinct from the git-tracked source and confirmed to survive a full `app.stop`/`app.start` cycle with prior request history intact.

Session ran with direct SSH access to the NAS (`truenas_admin@192.168.0.133`, a dedicated key generated for this session and added to that account, sudo password supplied by the user for the one-time filesystem setup) — this stage's validation is against the real box throughout, not a simulated stand-in.

**Real bug found and fixed, worth flagging for Stage 6:** the first working deploy bind-mounted the frontend's static assets as individual per-file mounts (`index.html`, `manifest.json`, the icons — mirroring the Dockerfile's own `COPY` list exactly). This looked right and passed an initial reachability check, but a live-edit test — editing `index.html` directly on the deployed-copy mount and re-requesting, the exact check this stage's deliverables call for — showed the container still serving the *old* content. Root cause: Docker's per-file bind mount pins to the source file's inode at mount time; `sed -i` (and, critically, `git`'s own checkout mechanism) replaces a file by writing a new one and renaming it into place, which changes the inode — the bind mount doesn't follow. Since Stage 6's entire deploy model *is* `git pull` on this exact mount, this would have silently broken "frontend picks it up on next request" the first time a real deploy touched `index.html` — caught here instead of in Stage 6. Fixed by switching to one directory-level bind mount (`frontend/:/usr/share/nginx/html`) — a directory mount re-resolves paths per request, so it isn't affected by the same rename pattern. That directory also holds `frontend/Dockerfile` and `frontend/nginx.conf`, which would otherwise become world-servable static files; added an explicit `deny`/404 `location` block in `nginx.conf` for both, verified live (`curl` returned `404` for each, `200` for the real homepage and the `/api/*` proxy). Also confirmed, in the course of fixing this: `app.update` and `app.redeploy` do **not** rebuild a `build:`-context custom app's image just because its build-context source changed (only a real `docker compose build` does) — so this same nginx.conf fix silently failed to take effect on the first two redeploy attempts until that was understood; a manual `docker compose build --no-cache` (via the same sudo access used for the initial filesystem setup) was needed to actually bake the fix in. This extends Stage 6's existing open decision about `requirements.txt` changes needing a manual image recreate: **any change to `frontend/Dockerfile` or `frontend/nginx.conf` (or `backend/Dockerfile`/`requirements.txt`) needs a manual rebuild — Stage 6's plain `git pull` endpoint only ever covers `backend/app/` and `frontend/`'s served content, both confirmed live-reloading correctly.**

Validated end-to-end against the real deployed stack, not a proxy or simulated stand-in: homepage and `/api/discover/popular` both reachable at `http://192.168.0.133:8095/` from another machine on the LAN (the closest available stand-in for "an iPad on the LAN" — no physical iPad was available this session either, same caveat Stage 4 flagged); a full `POST /api/requests` → `GET /api/requests/{id}` round trip using Andrei Rublev (895, the same known-no-qualifying-match title used for exactly this purpose since Stage 2) ran the complete real pipeline against the real qBittorrent instance through the real deployed containers, reaching `queued → searching → "no qualifying results"` correctly; and a full `app.stop`/`app.start` cycle confirmed that request's row was still readable afterward, proving the persistent-volume/restart-survival architecture decision holds on real TrueNAS storage, not just Docker Desktop. `backend` reaches the real qBittorrent app via the NAS's own LAN IP (`192.168.0.133:30024`, the existing `QBIT_HOST` default from every prior stage) rather than pure Docker-network DNS — qBittorrent is its own separate Custom App with its own compose project/network (`ix-qbittorrent`), so true same-network container DNS isn't available between separate apps in this app-store model; its WebUI is published on the host, and that's what every prior stage's manual testing already relied on too, so this isn't a new dependency, just confirmed to keep working unchanged from inside the new containers.

---

### Stage 6 — Git-based deploy & update
*Depends on: Stage 5's deployed-copy mount, proven to reload on file changes.*

**Purpose:** Replace hand-editing the deployed-copy mount with a deliberate `git pull` deploy step, plus the self-update endpoint.

**Stage 6 complete.** All deliverables done and live-verified on the real NAS:
- [x] Private GitHub repo (settled) with a read-only deploy key generated for the NAS specifically, stored as a mounted secret file — never in the git-tracked tree, never visible via `docker inspect`.
- [x] Rollback proven, not assumed: `git checkout <sha>` / `git revert` on the deployed-copy mount, confirmed to take effect.
- [x] Real `POST /api/admin/deploy`: exactly `git pull` (backend and frontend both — no build step for either) — no parameters, ever.
- [x] Gating: a plain, visible "Check for updates" button inside the Settings panel — changed from the originally-planned hidden long-press mid-stage, per explicit user direction favoring simplicity; the underlying accepted-risk call (no real auth) is unchanged.

**Settled:**
- Git remote: private GitHub repo + read-only deploy key.
- Gating: a visible button, no real auth. **Accepted risk:** anyone with the frontend open on the LAN can trigger a deploy; blast radius is bounded since the endpoint only ever runs a fixed `git pull`, never an arbitrary command.
- Frontend deploy is now as simple as the backend's — no build step, so `git pull` alone is the entire deploy for both.

**Open decisions:**
- [ ] Remember: `git pull` covers code changes but not new Python dependencies — a changed `requirements.txt` needs a manual image recreate via the TrueNAS UI, outside this endpoint's scope. Stage 5 found this also applies to `frontend/Dockerfile`/`nginx.conf` and `backend/Dockerfile`; this stage's own `backend/Dockerfile` change (adding `git`/`openssh-client`) needed exactly that manual rebuild to take effect, confirmed live below.

**Stage 6 built and verified live against the real TrueNAS box** (SSH access + `truenas_admin` sudo password granted this session, same pattern as Stage 5):
- `backend/app/deploy.py`: `run_git_pull()` — the entire self-update logic. Runs `git pull --ff-only` (deliberately not a plain `pull`: the deployed-copy clone is never committed to directly, so a non-fast-forward means something unexpected happened upstream — fail loudly rather than silently create a merge commit, the same "fail safe" principle the pipeline's matching logic already follows). Raises `DeployError` for a missing `.git`, a non-zero exit, or a timeout; `api.py` maps that to a 502. Reads `config.DEPLOY_REPO_PATH` (default `/repo`) and, if `config.GIT_SSH_KEY_PATH` is set, builds a `GIT_SSH_COMMAND` from it (`-o StrictHostKeyChecking=accept-new`, so the first real connection just TOFU-caches the host key).
- `POST /api/admin/deploy` (`backend/app/api.py`) now actually does this — no more 501 stub — still zero parameters, zero backend auth.
- `backend/Dockerfile` gained `git` + `openssh-client` (absent from `python:3.11-slim`) and `git config --global --add safe.directory /repo`, preempting git's dubious-ownership refusal on a bind-mounted, host-owned directory.
- `truenas/custom-app-compose.yaml`: backend gained a read-write mount of the *whole* deployed-copy clone (`.../repo:/repo`, the same host directory `backend/app`'s existing mount is a subdirectory of — a `git pull` against `/repo` updates both that mount and frontend's own directory-level mount, no separate sync step), a read-only mount of the deploy key (`.../secrets/deploy_key:/run/secrets/deploy_key:ro`), and `DEPLOY_REPO_PATH`/`GIT_SSH_KEY_PATH` env vars.
- Frontend: a plain "Check for updates" button in a new "Update" section of the Settings panel — `confirm()`, then `POST /api/admin/deploy`, success/failure both surfaced via `alert()`, button disabled and reads "Deploying…" mid-request.
- Tests: `backend/tests/test_deploy.py` exercises the real git plumbing against real, local, offline repos (a bare "origin" + a clone of it + a "seed" working copy used to push new commits) — up-to-date, fast-forward pull, non-fast-forward rejection, and not-a-repo, no mocking of `subprocess`. `test_api.py`'s stale `test_deploy_stub_returns_501` replaced with route-wiring/error-mapping tests via monkeypatch. 183 backend tests passing.
- Manual local smoke test before touching the NAS (no Docker on the workstation, matching every prior stage's validation-environment caveat): backend run locally against a disposable `git clone` of this repo's own working tree, frontend served through the same throwaway same-origin-proxy stand-in Stages 4/5 used — confirmed the long-press-era UI end-to-end (superseded by the button, same underlying call).
- **Live NAS rollout, this session:** generated the read-only deploy keypair directly on the NAS (`ssh-keygen` under `/mnt/SSD/AppData/movie-downloader/secrets/`, private key never left the box), user added the public half to the GitHub repo's Deploy Keys (read-only), switched the deployed clone's `origin` remote to SSH and confirmed a real `git fetch`/`git pull` over it authenticates with the deploy key. Found and fixed a real, unrelated gap while there: the deployed clone was 2 commits behind `origin/main` (still at `d5ffd59`) with an *uncommitted* local edit to `frontend/nginx.conf` — traced to Stage 5's live nginx-fix having been applied by hand on the NAS but never committed back (as that stage's own decision-log entry flagged). Confirmed the local edit was byte-identical to what `10ac106` (already pushed) already contained, so `git stash` + `git pull --ff-only` safely caught the clone up with zero risk of losing anything; the resulting empty/redundant stash entry was dropped later in the same session once confirmed safe. Applied the updated `truenas/custom-app-compose.yaml` via `midclt call app.update` (confirmed live: the backend container now reports all four expected mounts, including `/repo` and the deploy key) and rebuilt the backend image with `docker compose build --no-cache` + `up -d backend` — required because `backend/Dockerfile`'s new `git`/`openssh-client` line only takes effect on a real rebuild, not a recreate, exactly as this stage's own open-decision note above predicted.

**Validation — done, live, this session:** pushed the real Stage 6 commit (`48aad57`) to GitHub, pulled it onto the NAS's deployed clone with the real deploy key, rebuilt the backend image, then clicked the real "Check for updates" button on `http://192.168.0.133:8095/#/settings` in a real browser — real confirm dialog, real `POST /api/admin/deploy`, real response (`"Already up to date." @ 48aad57`). Rollback proven, not assumed: `git checkout 10ac106 -- frontend/index.html` on the deployed-copy mount, then a fresh page load confirmed the update button was genuinely gone (proving `git checkout`'s effect reaches the live site with no rebuild/restart, exactly as the confirmed architecture claims) — then `git checkout HEAD -- frontend/index.html` restored it, re-confirmed live.

---

### Stage 7 — Settings & preferences
*Depends on: Stage 3 (persistence), Stage 4 (UI to host it), Stage 6 (the endpoint the update button calls). No family profiles.*

**Purpose:** One shared settings panel behind a settings icon, for adjusting pipeline behavior and pushing a remote update — no login, no profiles.

**Deliverables / done when:**
- Settings icon opening a panel with: Stage 2's quality-tier weights/defaults, **minimum resolution floor (default 2160p/4K — lowering it to 1080p is what enables falling back to a lower resolution when nothing at 4K qualifies)**, size-range filter, language allow/block lists, category mapping, and (if Stage 8 wants it) Plex connection details.
- `GET`/`PUT /api/settings` backed by a settings table in the Stage 3 SQLite database — one row, not per-profile.
- The Stage 6 remote-update trigger ("Check for updates") lives inside this panel as a plain visible button; the rest of the panel is plainly visible and editable too.

**Open decisions:**
- [x] Whole-panel visibility: anyone on the iPad can open Settings (only the update trigger stays gated) — kept as-is, the same accepted-risk call Stage 6 already made for the update button (visible, no real auth; blast radius bounded). Nothing about a pipeline-preference edit is more dangerous than the update button already was, so there's no reason to gate this panel differently.
- [x] Basic sanity validation on settings edits — a `PUT /api/settings/pipeline` rejects (422) an unrecognized `min_resolution`, a blank `category`, and a size range where `min_size_gb >= max_size_gb`. That last check runs against the *effective* result of the edit (current row + this patch), not just the two fields when both happen to appear in the same request — a lone `min_size_gb` edit that would push it past whatever `max_size_gb` is already in effect (including the config.py default, if `max_size_gb` was never set) is caught too. Found this distinction by actually trying to break it live (see below) rather than reasoning it through in the abstract.

**Stage 7 complete.** New `backend/app/pipeline_settings.py`: a frozen `PipelineSettings` dataclass (`category`, `min_resolution`, `min_size_gb`, `max_size_gb`, `language_allowlist`, `language_blocklist`) plus `PipelineSettings.from_config()` (fresh from config.py every call — not captured once at import time, so every existing test that monkeypatches `config.MIN_RESOLUTION` etc. still passes unchanged) and `resolve_pipeline_settings(store)` / `settings_from_raw(dict)` (overlay the settings-table row on top of the config default, per-field — a null or absent key falls back, the same convention `accent_color`/`request_retention_days` already established). Deliberately narrower than the plan text's "quality-tier weights/defaults" might suggest: the actual resolution/source/codec/seeder/container point values (`score.py`'s `_RESOLUTION_WEIGHT` etc.) stay fixed, hardcoded, and untouched by this stage — they were reworked three times against real data during Stage 2 to keep one tier's difference always dominating the next, and a casual Settings-panel edit could silently break that domination-margin guarantee without any error to show for it. What's actually exposed is the one real per-household quality *preference* this pipeline has (the resolution floor) plus the other three deliverables the plan named explicitly by name (size range, language lists, category) — a scoping call made this session and worth flagging if it turns out to undersell what "quality-tier weights" was meant to promise.

`score.py`'s `passes_relevance_gate`/`passes_viability_gate` and `pipeline.py`'s `download()`/`_search_variant()` all gained an optional trailing `settings: PipelineSettings | None = None` parameter, defaulting to `PipelineSettings.from_config()` when omitted — this is what let every pre-existing test in `test_score.py`/`test_pipeline.py` keep passing without modification (monkeypatching `config.py` still works when no explicit settings object is passed), while `worker.py`'s `_run_one` now resolves real settings fresh from the store (`resolve_pipeline_settings(self.store)`) and passes them through explicitly on every request — so an edit takes effect on the very next request, no restart, no deploy. `cli.py` was deliberately left alone (still config.py-only, no store) — it's a dev tool, not something a Settings-panel edit needs to reach.

`api.py` gained `PipelineSettingsIn` (all six fields optional; the AppearanceSettings/RetentionSettings convention — a client always sends the complete desired state, `null` means "reset to this field's config.py default") plus `GET`/`PUT /api/settings/pipeline`. `frontend/index.html`'s Settings panel gained a "Download preferences" section between Appearance and Plex: a resolution dropdown, a size-range pair of number inputs, two comma-separated language-list text inputs, a category text field, and a Save button with an inline error banner for the client-side min-size/max-size check (mirroring the server's, so a bad edit doesn't round-trip through the API just to fail).

23 new backend unit tests (`test_pipeline_settings.py`, plus additions to `test_score.py`/`test_pipeline.py`/`test_api.py`), 206 total passing. Manually verified against the real NAS-hosted backend and real qBittorrent (no docker/nginx on this workstation, same caveat every prior stage has flagged — validated through a throwaway same-origin FastAPI+StaticFiles stand-in, not part of the repo, same pattern Stage 4/5 used): opened the real Settings panel in a browser, edited and saved every field, confirmed a bad size range showed the inline error and never reached the API, confirmed the backend's own 422s independently via curl for an unrecognized resolution, a blank category, and an inverted size range. Found the "effective range, not just the two fields together" gap this way — an isolated `{"min_size_gb": 500}` PUT (a real edit through the actual panel) sailed through validation because `max_size_gb` wasn't in that same request, then silently produced an unsatisfiable 500–150 GB range once `max_size_gb` fell back to its 150 GB default; fixed by validating the *merged* prospective state (current row + patch) rather than the raw patch fields in isolation, added a regression test built from this exact case, and reset the (local, dev-only — not the NAS's) settings row back to defaults afterward. Proved the settings-to-pipeline wiring live and read-only, no real torrent added: resolved Big Buck Bunny (tmdb_id 10378, the same disposable public-domain title Stage 3 used for exactly this reason) against the real qBittorrent search API — 8 candidates passed the relevance gate, 0 passed the viability gate at the default `min_size_gb=1`, 2 passed once `min_size_gb` was lowered to `0.1` through an explicit `PipelineSettings` object (the same object `worker.py` builds from the store), confirming a real Settings-panel-shaped edit changes real pipeline output without needing to trigger an actual add. Ready for Stage 8.

---

### Stage 8 — Hardening & observability
*Depends on: all prior stages. A pass, not new scope.*

**Purpose:** Right-sized visibility so failures don't just sit there silently.

**Deliverables / done when:**
- Structured logging to stdout (TrueNAS's own app log viewer) — one line per pipeline transition, including which variant matched and its score.
- `GET /api/admin/jobs?status=failed`, gated the same way as the deploy endpoint.
- Backend healthcheck (can it reach qBittorrent).
- Optional: push notification on failure (e.g. ntfy).
- Optional: Plex partial-scan on a request reaching `complete` — the "Add to Plex" button implies near-instant appearance, and a fresh download won't show up until something triggers a scan. Needs a Plex URL + token as a Stage 7 setting.

**Open decisions:**
- [x] Push-notification-on-failure (ntfy) — **declined.** Asked explicitly at the start of this session; the user chose to skip it and rely on the failed-jobs endpoint/logs instead. Not built.
- [x] Automatic Plex scan-on-complete — **declined**, same question. Left to Plex's own folder watching; not built. (Plex is still used exactly as before — `plex.has_in_library` as the cancellation/completion tiebreaker in `worker.py`, unchanged from before this stage.)
- [x] Log retention — kept as TrueNAS's own app-log-viewer default; no shipping pipeline built, per the plan's own steer.
- [x] Failed-jobs view — started (and left) as a raw JSON endpoint, no frontend UI, per the plan's own steer ("only build UI if it gets used often").

**Stage 8 complete.** New `backend/app/logging_config.py`: `configure_logging()` attaches one `StreamHandler(sys.stdout)` to the `"app"` logger (the shared parent of `"app.worker"`, `"app.api"`, etc.), sets `propagate = False` so output is identical and never doubled regardless of whether the process is started under uvicorn, gunicorn, or plain `python`, and is a no-op if called twice (guards against duplicate handlers on reimport). Called once at `api.py` module load.

`backend/app/worker.py` gained one `logger.info` line per pipeline transition that didn't already have one: the pipeline's raw result (status, which variant matched, candidates considered, and the full score breakdown via a new `_score_summary()` helper) at the point `_run_one` decides the request's new status; `downloading -> complete` (both the plain progress-reaches-1.0 path and the Plex-confirmed-after-disappearance path, distinguished in the log line); and `downloading -> cancelled` (both the automatic disappearance path and the message it settled on). `backend/app/api.py`'s manual `POST /api/requests/{id}/cancel` route gained the same treatment. Boot recovery and retention purges were already logged (Stage 3/7) and needed no changes.

`backend/app/qbt.py` gained `QBTClient.ping()` — a cheap `app_version()` call wrapped in `try/except Exception: return False`, since the client only proves qBittorrent was reachable at *construction* time (`auth_log_in()` in `__init__`), and a healthcheck needs to know it's still reachable right now. `backend/app/api.py` gained `GET /api/health`, returning `{"status": "ok", "qbittorrent": <bool>}` — deliberately always `200` even when qBittorrent is unreachable, since "this backend process is up" and "an external dependency is temporarily down" are different facts, and conflating them (e.g. failing the healthcheck and letting some orchestrator restart the backend over a qBittorrent blip) would violate "fail safe, not best guess." No Dockerfile `HEALTHCHECK` directive was added — this stage only builds the endpoint the plan asked for; wiring it into container-level health monitoring wasn't part of the deliverables and wasn't asked for.

`backend/app/api.py` also gained `GET /api/admin/jobs?status=...` — gated exactly like `/api/admin/deploy` (visible, no real auth, meant to be checked with `curl`). With no `status` given it returns every row in `{"failed", "no qualifying results", "insufficient free space"}` (deliberately excluding `"cancelled"`, which is far more often a user's own choice than a failure worth surfacing here); passing `status` narrows to exactly one. Reuses the existing `RequestOut` shape, so the JSON matches `/api/requests` exactly.

7 new backend unit tests (213 total passing): `test_qbt.py` covers `ping()`'s success/failure paths against a fake underlying client (same `object.__new__(QBTClient)` pattern Stage 2's `add_torrent` tests already used, skipping the real `auth_log_in()`); `test_worker.py` asserts the pipeline-result log line actually carries the variant and score, and that the download-watcher's complete transition logs; `test_api.py` covers `/api/health` in both the reachable and unreachable cases (confirming the latter still returns `200`, not a failure status), and `/api/admin/jobs`'s default multi-status filter plus its single-`status` narrowing.

No SSH/NAS access used or needed this session — everything validated against the local test suite (mocked TMDB/qBittorrent, same as every other API/worker test) rather than the real NAS-hosted stack, since this stage is pure observability plumbing with nothing new to cross-check against live search/scoring data the way Stages 2/3/7 needed to. Nothing in this session touched the real NAS deployment — both Stage 7 and Stage 8's code need a `git pull` (or "Check for updates") on the deployed clone before the live app reflects either.

**Validation:** Deliberately induce failures — kill qBittorrent mid-request, request something with no plausible matches, restart the backend mid-job — confirm each is diagnosable from logs/endpoint without SSHing in. Covered by the unit tests above (fake-qBittorrent unreachability for `/api/health`, a genuinely-failed/no-match/no-space row appearing in `/api/admin/jobs`, and the pipeline-result log line firing on every outcome) rather than a live induced-failure pass against the real NAS, per the "no SSH access this session" note above — a real live pass (kill the actual qBittorrent app mid-request, watch `/api/health` flip and the transition log appear in `docker logs`/TrueNAS's app log viewer) is still worth doing once this is deployed.

---

### Stage 9 — TV discovery & episode resolution
*Depends on: none beyond the existing TMDB client. Mirrors Stage 1, for shows.*

**Purpose:** Everything the iPad UI needs to browse/search TV shows is TMDB, proxied through the backend, plus resolving one selected show into an identity and a per-episode search query — the Stage 1 equivalent for shows, not movies.

**Deliverables / done when:**
- `backend/app/tmdb.py` gains `/tv` wrappers: `search_tv`, `get_tv(tmdb_id)` (append_to_response credits), `get_tv_season(tmdb_id, season_number)` (episode list with `air_date`), `get_tv_popular`/`get_tv_trending` (TTL-cached, same `ttl_cache` decorator Stage 1 already built).
- New `backend/app/tv_resolve.py`: `ShowIdentity` dataclass (tmdb_id, title, original_title, variants) — reuses `resolve.py`'s `generate_variants`/subtitle-splitting logic (extracted to be shared, not copy-pasted, per "one normalizer, many uses"). `episode_query(title_variant, season, episode) -> str` builds `"{variant} S{season:02d}E{episode:02d}"`.
- CLI: `resolve-show <tmdb-id>`.

**Open decisions:**
- [x] Episode token format: `S01E04` only for v1 — the dominant modern scene/indexer convention. Alternate formats (`1x04`, `Season 1 Episode 4`) are a named, documented gap, same style as Stage 2's cam-tag gap — not solved here, revisit only if it turns out to matter against real search results.

**Validation:** Unit tests on variant/episode-query generation. Manual CLI runs against real TMDB for a currently-airing show (season/episode data, air dates).

**Stage 9 complete.** `backend/app/tmdb.py` gained `search_tv`, `get_tv` (`append_to_response=credits`), `get_tv_season(tmdb_id, season_number)` (returns just the episode list, each carrying `episode_number`/`air_date`), and TTL-cached `get_tv_popular`/`get_tv_trending` — same `ttl_cache` decorator and 300s window Stage 1 already built, no new caching mechanism. New `backend/app/tv_resolve.py`: `ShowIdentity` dataclass (`tmdb_id`, `title`, `original_title`, `variants` — deliberately no `release_year` field, since a show has no single release year the way a movie does; the episode token is the tighter matching signal for Stage 10 anyway), `episode_query(title_variant, season, episode) -> str` (`"{variant} S{season:02d}E{episode:02d}"`), and `resolve_show(tmdb_id, client)`. `cli.py` gained `resolve-show <tmdb-id>`.

One refactor beyond the plan's literal deliverables list, matching what the TV-planning session's decision-log entry already called for: `generate_variants`/`_title_without_subtitle` moved out of `resolve.py` into `normalize.py` (alongside the token utilities they already depend on, per "one normalizer, many uses") so `tv_resolve.py` reuses the exact same function rather than a copy — `resolve.py` now just imports it, and every pre-existing `test_resolve.py` test still passes unchanged since it imports `generate_variants` from `app.resolve`, which still re-exposes it. `resolve_show` passes `release_year=None` into the shared generator, so a show only ever gets a title variant, an original-title variant (if different), and a subtitle-free variant — never a "+year" variant.

14 new unit tests (`test_tmdb.py`'s new TV-wrapper cases, `test_tv_resolve.py`), 227 total passing. Manually verified live against real TMDB: `resolve-show 1399` (Game of Thrones, no subtitle) → 1 variant; `search_tv("Lanterns", year=2026)` found the real show (tmdb_id 95350) among 2 results; `resolve-show 95350` → 1 variant (`Lanterns`); `get_tv(95350)["number_of_seasons"]` → 1, and `get_tv_season(95350, 1)` returned 8 real episodes with real air dates — as of this session's date (2026-09-06), 4 have already aired (through "The Weenie," 2026-09-06), which happens to line up almost exactly with the plan's own "Lanterns" worked example from the 2026-09-04 decision-log entry (add a show with only some episodes aired, auto-grab the next one). The subtitle-splitting case (`Star Trek: Discovery` -> also `Star Trek`) and the original-title-differs case (Attack on Titan / 進撃の巨人) weren't re-run live — both reuse `normalize.py`'s already-live-tested (Stage 1) splitting/dedup logic verbatim, so they're covered by `test_tv_resolve.py`'s fixture-based unit tests instead of a second live call. Ready for Stage 10.

---

### Stage 10 — Episode-aware match, score & download
*Depends on: Stage 9. Mirrors Stage 2, reusing its pass-two scorer unchanged.*

**Purpose:** Confirm a search result is actually the specific episode Stage 9 resolved (not just the right show, and not a season pack), then rank survivors with the exact same quality scorer movies already use.

**Deliverables / done when:**
- New pass-one episode-identity check (in `score.py` or a small `tv_score.py`): whole-token exact match on the `s01e04`-style token via the existing `tokenize`/`has_token` utilities — no year-tolerance check for TV (an episode's air-year can legitimately differ from the show's first-air-year; the episode token is already a tighter signal than year ever was for movies). Resolution floor, language allow/blocklist, and the cam blocklist are reused unchanged from `score.py`.
- Refactor: extract the "rank → filter-to-free-space → add → capture-hash-with-retry" loop out of `pipeline.py`'s `download()` into a shared helper, so a new `download_episode(show_identity, season, episode, tmdb, qbt, settings)` reuses it instead of duplicating that (nontrivial, bug-prone-in-the-past per Stage 3's hash-capture fix) logic.
- CLI: `download-episode <tmdb-id> <season> <episode>`.

**Settled:**
- **Season packs are out of scope for v1.** A season-pack torrent covers many episodes at once, which doesn't fit the per-episode dedup ledger (Stage 12) cleanly — one add would need to retroactively mark several episodes "complete." Documented as a future stage if wanted, not solved here, matching the project's existing pattern of naming known gaps rather than papering over them. A solo season-pack-shaped result (an `s01` token with no episode token) is explicitly rejected by the pass-one gate, not silently accepted as an episode match. It also sidesteps the file-organization problem Stage 11 solves — a season pack's files don't map one-to-one onto "one episode, one target path" the way a per-episode download does.

**Validation:** Unit tests mirroring Stage 2's — fixture result sets including a season-pack fixture that must be rejected, messy `nbSeeders`/`fileSize` cases reused as-is. Manual CLI run against a real airing show's most recent episode.

---

### Stage 11 — Post-download file organization & Plex library layout
*Depends on: Stage 10 (needs a real completed episode torrent, plus the show/season/episode identity Stage 9/10 already resolved for it). Closes a real gap the user flagged after Stage 9 shipped: an episode downloaded on its own, under whatever name its release used, doesn't land anywhere Plex's TV agent reliably recognizes as "this show, this season, this episode" — especially across a season's worth of episodes each coming from a differently-named release.*

**Purpose:** Make a completed episode download show up in Plex, in the right show's folder and the right season, without Plex ever having to guess which show a scene-release filename refers to. The key move: this pipeline already knows exactly which show/season/episode a download is for — it resolved that identity *before* searching (Stage 9/10) — so placement is a deterministic write to a known path, not a filename-matching heuristic layered on afterward the way a general-purpose media manager has to do it.

**Deliverables / done when:**
- New `backend/app/media_organizer.py`: `select_video_file(qbt, torrent_hash) -> Path` picks the real episode file out of a completed torrent's file list — the largest file with a known video extension, skipping anything named/sized like a sample. Season packs never reach this code (Stage 10 rejects them in pass one), so "one torrent → one episode's file" is the only case this has to handle — a named, documented gap if a release's structure doesn't fit that assumption, same style as Stage 2's cam-tag gap.
- `organize_episode(show_identity, season, episode, source_path) -> Path`: builds the target path `<TV_LIBRARY_ROOT>/<Show Title> ({year}) {tmdb-<tmdb_id>}/Season <NN>/<Show Title> - s<NN>e<NN>.<ext>` — using Plex's own documented `{tmdb-<id>}` folder-naming hint, so Plex is never left to guess which show a folder belongs to at all. Creates the show/season folders if missing, then **hardlinks** (never moves or renames in place) the source file into that path — the same safe pattern Sonarr/Radarr rely on: qBittorrent's own copy keeps seeding, completely untouched, while Plex sees a second, correctly-named reference to the same bytes at zero extra disk cost. Falls back to a plain copy (logged loudly — it doubles disk usage) only if the target path turns out to be on a different filesystem than the source, where a hardlink can't be created at all.
- `worker.py`'s existing download-watcher calls this exactly once, at the same point a `downloading` row would flip to `complete` — gated on `media_type == 'episode'` only. Nothing changes for movies; qBittorrent alone remains fully responsible for them, exactly as today.
- A new status, `"downloaded, not filed"` — distinct from `complete`, used only when the torrent itself genuinely finished but organizing failed (file-selection ambiguity, missing bind mount, hardlink/copy error). Matches the project's existing pattern of adding a genuinely-distinct status rather than conflating "done" with "needs a human" (the `"insufficient free space"` precedent from Stage 2/3) — an episode a `complete` row claims is watchable actually has to be, per "hidden, never unrecoverable." Surfaced through the existing `/api/admin/jobs`-style raw-JSON visibility (Stage 8) — no new frontend UI, same steer as every other admin-visibility deliverable so far.
- New infra dependency, called out rather than assumed: the backend container needs a **new bind mount** into the same underlying TrueNAS dataset qBittorrent downloads into *and* Plex's TV library already lives on — same filesystem, or the hardlink step above can't work at all. `truenas/custom-app-compose.yaml` gains this mount; the real host path is a build-time NAS detail, not something this planning pass can pin down.
- `config.py` gains `TV_LIBRARY_ROOT` (this container's path to the library root) and a small video-extension/sample-skip constant list — plain module constants for now, matching how Stage 2 introduced its own tuning knobs ahead of any Settings-panel exposure.

**Settled:**
- **Rename/place only after the torrent is fully complete, never mid-download.** The user's own framing ("change the file names once they start downloading") is solved differently on purpose: touching qBittorrent's in-progress file risks its resume data and its own incomplete-file naming, for no benefit — a hardlink placed the moment the download finishes achieves the same practical outcome (Plex sees a correctly-named, correctly-placed file) without ever going near the live download.
- **Identity comes from this app's own resolved metadata, never from re-parsing the downloaded filename.** The show/season/episode a `requests` row already carries (Stage 9/10's resolution, persisted per Stage 12's schema) drives the target path directly — this is what actually closes the "is the correct show detected" concern: Plex is handed an unambiguous `{tmdb-<id>}` folder hint instead of being asked to infer anything from a scene-release name.
- **No new Plex scan trigger.** Stage 8 already declined an automatic scan-on-complete for movies and leaned on Plex's own real-time folder-watching instead; this stage keeps that call for TV too, since the actual bug being fixed is that the file was never somewhere Plex could recognize in the first place — once placement/naming is right, the existing folder-watching this project already depends on should just work.

**Open decisions:**
- [ ] Episode title in the filename (`Show - s01e04 - Episode Title.ext`, needing one more TMDB lookup per organize) vs. `SxxEyy`-only (`Show - s01e04.ext`, both fully valid to Plex) — proposing `SxxEyy`-only as the simpler v1 default; revisit once real episodes are flowing if it turns out to matter for browsing inside Plex itself.
- [ ] The real shared-dataset host path on the NAS, and whether qBittorrent's existing save path already lives on the same filesystem as Plex's TV library or needs to move first — a real-box question for whoever builds this stage, not guessable here.
- [ ] Hardlink-fails-across-filesystems fallback: silently copy (simple, costs disk space) vs. treat it as a hard failure into `"downloaded, not filed"` (matches "fail safe, not best guess" more literally, but might be too strict if the real layout turns out to need it often) — left open pending what the real dataset layout turns out to be.

**Validation:** Unit tests for file-selection (a multi-file torrent fixture with a sample to skip), path-building (season zero-padding, the `{tmdb-<id>}` folder hint, a specials/season-0 case), and the hardlink/fallback-copy logic against a real local temp filesystem. Manual pass against the real NAS once Stage 10/12 can produce a genuine completed episode: confirm the file lands in the right show/season folder under the right name, and that Plex actually picks it up on its own without a manual "Scan Library Files."

---

### Stage 12 — Subscriptions, scheduling & per-episode dedup
*Depends on: Stage 10. The core architectural-gap stage the 2026-09-04 decision-log entry flagged: everything built through Stage 8 is a one-shot request, and "watch this show" needs a standing subscription that outlives one. Pairs with Stage 11 — a scheduled catch-up download is only actually useful once it also lands somewhere Plex will show it.*

**Purpose:** A show, once added, keeps itself up to date — newly aired episodes get found and downloaded on a schedule, without anyone re-opening the app, and without ever re-searching or re-adding an episode already handled.

**Deliverables / done when:**
- Two new SQLite tables (`db.py`): `shows` (id, tmdb_id, title, status `watching`/`paused`, created_at, last_checked_at) — the standing subscription; `show_episodes` (id, show_id, season_number, episode_number, request_id, created_at, `UNIQUE(show_id, season_number, episode_number)`) — the dedup ledger, distinct from the audit trail.
- `requests` table gains nullable `media_type` (default `'movie'`), `show_id`, `season_number`, `episode_number` columns — an episode's actual search/score/add audit trail is a normal `requests` row, reusing every existing status (`queued` → … → `complete`/`failed`/etc.), the download watcher, retention purge, and the cancel endpoint completely unchanged.
- `worker.py` gains a 4th background loop, `_watch_shows` (same `asyncio.sleep(interval)` pattern as `_watch_retention`): for each `watching` show, fetch current season/episode data from TMDB, diff against `show_episodes`, and for every aired-but-not-yet-dedup'd episode: create a `requests` row (`media_type='episode'`), insert its `show_episodes` marker, and `enqueue()` it into the **same** queue/pipeline lock movies already use — no second lock, no distributed queue.
- `POST /api/shows` (subscribe) runs this exact same check logic immediately, so it catches up every already-aired episode — "add show mid-season" and "scheduled recheck" are the same code path, not two.
- `POST /api/shows/{id}/pause`, `/resume`; `DELETE /api/shows/{id}` (unsubscribes — stops future checks, keeps all history rows, same "hidden, never unrecoverable" principle as cancelling a movie request).

**Settled:**
- **Confirmed architecture needs no change**, closing out the re-litigation the deferred prompt explicitly asked for. The single `asyncio.Lock` still serializes all actual search/score/add work (movie and episode requests share one queue); SQLite persistence is just two more plain-`sqlite3` tables; still no distributed job queue. The scheduler loop itself doesn't touch qBittorrent (it only reads TMDB and diffs against `show_episodes`), so it never contends with the pipeline lock — it just calls `enqueue()`, exactly like `Worker.start()`'s boot-time re-enqueue of leftover `queued` rows already does today.
- **Catch-up on add is the only behavior** — adding a show always backfills every already-aired, not-yet-downloaded episode. No "watch from now on only" toggle in v1 — matches the deferred prompt's own worked example (add "Lanterns" with only 3 episodes aired, auto-grab episode 4).
- **Specials (season 0) excluded by default.**
- **Which seasons get checked each cycle:** the latest season only, plus re-reading `number_of_seasons` from `/tv/{id}` each cycle to notice a new season announced/started. An older season resuming after a long hiatus is a known, accepted gap.

**Open decisions:**
- [ ] Check interval: proposed as one global `config.py` constant (`SHOW_CHECK_INTERVAL_SECONDS`, default 6 hours), not a per-show or Settings-panel value in v1 — matching "household scale" simplicity elsewhere. Left open rather than settled, since it trades off promptness against indexer request volume in a way that's genuinely a household preference, not an engineering call — revisit before or during Stage 12's build if the default feels wrong.

**Validation:** Unit tests for the diff/dedup logic (fixture: a show with some episodes already in `show_episodes`, some newly aired, some unaired), the pause/resume/unsubscribe state transitions, and add-triggers-catchup. Manual run against a real currently-airing show on the NAS.

---

### Stage 13 — Whole-season & complete-series bulk acquisition
*Depends on: Stage 11 (its per-episode file-organizer, extended here to handle many files from one torrent) and Stage 12 (its `show_episodes` dedup ledger, backfilled here so a later per-episode catch-up never re-fetches what a pack already covered). Addresses a real gap the user flagged: a show that's been over for years is exactly the case where per-episode auto-grab (Stage 12) is the wrong tool — hammering an indexer with one search per episode across, say, 8 seasons is slow and often comes back with poorly-seeded (or no) single-episode results, when a well-seeded season or complete-series pack is frequently the only realistic way to get an old show at all.*

**Purpose:** A second, deliberately distinct acquisition mode — "get this whole season" or "get the complete series" in one user-triggered action, as an alternative to (not a replacement for) Stage 12's per-episode standing subscription. Explicitly **not** reopening Stage 10's settled decision that a pack-shaped result is rejected during *episode* search — that gate is unchanged and still protects the per-episode auto-grab loop from ever mis-filing a pack as a single episode. This stage adds its own opposite-shaped gate, used only when the user has explicitly asked for a bulk download, never automatically.

**Deliverables / done when:**
- New pass-one gate (in `score.py` or a small `pack_score.py`) that specifically wants a season-or-more-shaped result: a season token (`s01`, `season 01`) present with **no** accompanying episode token, or an explicit multi-season/complete-series marker (`complete series`, `the complete series`, a season-range token like `s01-s03`). Reuses the existing whole-token utilities unchanged — this is the mirror image of Stage 10's existing "reject a lone season token" rejection, applied deliberately here instead of avoided.
- Query variants built from Stage 9's existing title variants, each tried with a scope suffix: `"{variant} complete series"` for a whole-series request, `"{variant} season {n}"` / `"{variant} S{n:02d} complete"` for a single season — mirrors `tv_resolve.py`'s `episode_query` pattern, just a different suffix shape.
- Scope detection on the candidate set: a v1-simplified classifier that recognizes a single season, or an explicit "complete series" marker, as the only two supported shapes. A partial multi-season range that isn't the full series (`S01-S03` of an 8-season show) is a named, documented gap for v1 — not solved here, same style as Stage 2's cam-tag gap and Stage 9's episode-token-format gap.
- Same pass-two composite scorer as everything else (`score.py`, unmodified) ranks pack candidates exactly like any other release — resolution/source/codec/seeder/container, no pack-specific weighting.
- `media_organizer.py` (Stage 11) gains `organize_pack(show_identity, expected_seasons, torrent_hash)`: enumerates every file in the completed torrent, and for each one that carries its own recognizable `SxxEyy` token, calls the *same* per-file placement logic `organize_episode` already does (season/episode token from the filename itself this time, not from a single fixed request) — so this is an extension of Stage 11's existing function, not a parallel implementation. A file with no recognizable episode token (samples, NFO, subtitle sidecars, an unmatched extra) is skipped and logged, not treated as an error.
- Fan-out on add: one pack torrent add creates **one `requests` row per episode found in it** (all sharing the same `torrent_hash`, `media_type='episode'`) rather than one opaque "pack" row — this is what lets the existing requests list, retention purge, admin/jobs visibility, and per-row `complete`/`"downloaded, not filed"` status all keep working completely unchanged, the same reuse-not-duplicate call Stage 12 already made for scheduled per-episode adds. The one new nuance: the download-watcher's progress poll (keyed on `torrent_hash`) now has to flip a *set* of rows at once instead of one — a straightforward one-to-many update, not new logic.
- Every successfully-organized episode gets a `show_episodes` row inserted (Stage 12's dedup ledger) the moment its file is placed — so a show bulk-downloaded today and subscribed-to tomorrow doesn't have Stage 12's catch-up-on-add logic try to re-search and re-add everything the pack already covered.
- `POST /api/shows/{id}/bulk-download` (body: `{"scope": "series"}` or `{"scope": "season", "season_number": N}`) — a plain, explicit user action, distinct from `POST /api/shows` (subscribe/watch). Usable regardless of the show's own airing status: an ended show is the motivating case, but nothing stops using it to backfill seasons 1-5 of a currently-airing show while staying subscribed for new episodes as they air — both mechanisms can run against the same show at once, since they only ever interact through the shared `show_episodes` ledger.

**Settled:**
- **Bulk acquisition and the standing subscription are independent, not mutually exclusive.** A show can be bulk-downloaded, subscribed-to, both, or neither — `shows`/`show_episodes` don't distinguish how an episode's dedup marker got there.
- **Only two pack shapes supported in v1: one full season, or the complete series.** A show-runner's real-world messy middle ground (an arbitrary season range, a "seasons 1-4" pack for an 8-season show) is out of scope, matching the project's running pattern of naming a known gap rather than half-solving it.
- **No new frontend surface in this stage** — the actual "Download complete series" / "Download season N" controls are Stage 14's job, same split as every other backend-then-frontend stage pairing so far.

**Open decisions:**
- [ ] What happens when a pack only partially matches `expected_seasons` (e.g. a "complete series" pack that's actually missing the newest season TMDB already knows aired) — accept the partial result and dedup only what was actually found, or reject the whole candidate and fall through to the next variant? Leaning toward "accept what's actually there" (matches "fail safe, not best guess" — never claim something was grabbed that wasn't), but left open for the build session.
- [ ] Whether a `pack_score.py` split file is worth it or this folds into `score.py` directly — a real code-organization call better made with the actual pass-one logic in front of someone, not guessed at here.

**Validation:** Unit tests mirroring Stage 10's — fixture pack results (a real-shaped complete-series listing, a single-season pack, and a plain season-pack-with-no-episode-token that must still be *accepted* here even though Stage 10 rejects the identical shape in its own context, proving the two gates are genuinely independent) — plus a multi-file organize test (a fixture torrent with several `SxxEyy`-tagged files and one unmatched extra) confirming every real episode gets its own row, its own placed file, and its own `show_episodes` entry, and the extra is skipped without error. Manual run against a real, actually-ended, low-stakes show on the NAS, confirming a bulk season download produces one correctly-organized file per episode and the requests list shows them individually.

---

### Stage 14 — TV frontend & API surface
*Depends on: Stage 12, Stage 13.*

**Purpose:** Expose TV browsing and subscription management on the iPad — distinct from movies' one-shot "Add to Plex," since a show is an ongoing relationship, not a single request.

**Deliverables / done when:**
- `GET /api/tv/discover/{popular,trending}`, `GET /api/tv/{tmdb_id}` (thin TMDB pass-throughs, same key-never-reaches-the-browser rule as movies).
- `GET`/`POST`/`DELETE /api/shows[/…]`, `POST /api/shows/{id}/pause|resume` — thin wrappers per Stage 12. `POST /api/shows/{id}/bulk-download` — thin wrapper per Stage 13.
- `frontend/index.html`: a TV browse surface (popular/trending grid + search, mirroring the movie home grid), a show detail view with a three-state button (**Add Show** / **Watching ✓** / **Paused**, replacing movies' one-shot Add-to-Plex/Added) *plus* a separate **Download complete series** action, and — once the show has more than one season — a per-season **Download this season** control (both wired to Stage 13's `bulk-download` endpoint, available regardless of watching/paused state, per that stage's "independent, not mutually exclusive" call). A show whose TMDB `status` is `Ended`/`Canceled` surfaces the complete-series action more prominently, since that's the case it's actually for — but it's never hidden for an airing show, since backfilling older seasons while staying subscribed for new ones is a real, supported combination. A **Watching** list view (each show's last-checked time and latest episode status). The existing requests list already shows episode rows for free once `media_type`/`show_id`/season/episode are in the API response — it just needs a label change (`"{Show} S01E04"` instead of title/year) for episode rows, whether that row came from the per-episode scheduler or a bulk pack add.

**Validation:** Manual walkthrough — browse TV, add a show with some episodes already aired, confirm catch-up requests appear in the requests list and actually add real torrents (a disposable/low-stakes show, same pattern every prior stage's live validation used), pause a show and confirm no new requests appear on the next scheduled check, unsubscribe and confirm history rows survive; separately, trigger a bulk season/series download on an ended, low-stakes show and confirm the requests list shows one row per episode, all correctly organized.

---

## Non-goals (this pass)

- No MVP code, no scaffolding, no repo structure commitment beyond what's stated above.
- No CI/CD pipeline, no Kubernetes, no auth framework.
- No Docker socket in the backend, under any stage.
- No distributed job queue — the single `asyncio.Lock` is treated as correct at household scale throughout.

---

## Decision log

*Append one entry per stage session, newest last. This is how context survives across separate chats.*

- **2026-09-04** — Plan approved. Compartmentalization approach adopted: this file is the shared plan, each stage gets its own chat.
- **2026-09-04** — Stage 0 spike run against the real NAS-hosted qBittorrent (v5.2.3, 21 plugins enabled) and a live TMDB key. Full findings: [spikes/stage0-feasibility.md](spikes/stage0-feasibility.md). Headline results: plan's core assumptions survive, with two adjustments carried into Stage 2 — (1) plugins can return error/config rows disguised as results with a populated but bogus `fileUrl` (seen from a misconfigured `jackett` plugin pointing at its own local API) and must be excluded, not just field-validated; (2) `nbSeeders` is `-1` (unknown) on ~50% of real rows, almost entirely from one high-volume plugin (torlock), confirming seeders must stay a tiebreaker/gate, never the primary key. `fileSize` was reliable (98%+ valid). TMDB search/alternative_titles/discover-by-provider/popular all matched the assumed contract; alternate-titles data for a franchise title (Dune: Part Two) was rich (35 entries) but mostly non-Latin-script noise, confirming the plan's small-curated-variant-list approach over using every AKA.
- **2026-09-04** — Stage 1 built and verified. New `backend/` Python package (venv + `requirements.txt`: `requests`, `python-dotenv`, `pytest`) holding the pipeline-as-a-library code: `app/normalize.py` (whole-token normalizer, shared with Stage 2), `app/cache.py` (generic TTL-cache decorator), `app/tmdb.py` (TMDB client — search/get_movie/alternative_titles uncached, popular/trending/watch_providers/discover_by_provider TTL-cached at 300s), `app/resolve.py` (`MediaIdentity` dataclass + `generate_variants`, capped at 4: title, original_title if different, subtitle-free title, title+year), `app/cli.py` (`resolve <tmdb-id>`). TMDB key stored in gitignored `backend/.env`, loaded server-side only via `python-dotenv` — confirmed via repo-root `.gitignore`. 14 unit tests pass (normalization edge cases — diacritics, whole-token vs. substring like `265` inside `x265`/`1265`; variant generation for franchise/subtitle/no-subtitle/no-year cases). Manually exercised against live TMDB: `resolve 693134` (Dune: Part Two) returned 3 variants; direct client calls for search, alternative_titles (35 entries), popular, watch_providers (Netflix id 8), and discover_by_provider (4,748 total results) all matched Stage 0's numbers, and the TTL cache was confirmed not to grow on a repeat call. No deviations from the plan; the one open decision (in-memory vs. persisted cache) was closed in favor of the plan's stated default. Ready for Stage 2.
- **2026-09-04** — Stage 2 built and verified. `requirements.txt` gained `qbittorrent-api`; `app/config.py` gained qBittorrent connection settings (defaulting to the real NAS instance from Stage 0, `192.168.0.133:30024`, no WebUI auth needed on this network — confirmed again live) and the pipeline defaults (category, resolution/year/language gate settings, source/codec/container tier tables, size range, min seeders, plugin distrust list) as plain module constants ahead of Stage 7's settings table. New: `app/qbt.py`, `app/score.py`, `app/pipeline.py` — see the Stage 2 section above for what each does and all four of its open decisions being settled. 51 unit tests pass, all four Stage 2 open decisions closed as described above, and two manual CLI runs confirmed the pipeline end-to-end against the real NAS: `download 693134` (Dune: Part Two) matched, scored, and added a real torrent — a genuine side effect flagged to and kept by the user rather than deleted — and `download 895` (Andrei Rublev, deliberately unmatchable at 4K) correctly returned `"no qualifying results"` after exhausting its variants. One addition beyond the plan's literal deliverables list: a free-space gate before adding, using the `free_space_on_disk` read that Stage 2 had already "Settled" as a data source but the deliverables didn't explicitly wire in — added now rather than left dangling, with a new `"insufficient free space"` status distinct from `"no qualifying results"`.
- **2026-09-04** — Same session, before moving to Stage 3: the user asked whether 1080p would ever be a frontend setting with automatic fallback when no 4K result is found — reopening "the biggest open call in the plan" (the hard 2160p/4K gate), which Stage 2 had just built and live-tested around. Agreed to fold it into the plan immediately rather than defer to Stage 7. Reworked `backend/app/config.py`/`score.py`/`pipeline.py`: resolution moved from a fixed pass-one token check to a `RESOLUTION_TIERS` table (2160p/4K(4) > 1080p(3) > 720p(2) > 480p(1)) plus a `MIN_RESOLUTION` floor setting (default `"2160p"`, so current behavior is unchanged until someone lowers it), with resolution folded into pass two's composite as the highest-weighted term (`resolution_score*1000 + source_score*100 + codec_score*10 + container_score*1`) so a qualifying 2160p candidate always outranks a qualifying 1080p one, but a 1080p candidate is still picked when it's all that's available under a lowered floor — no separate fallback search pass needed, it falls out of one ranked pool per variant. Added `resolution_score` to `Score` and the CLI's audit-trail printout. 10 new unit tests (`backend/tests/test_score.py`, `test_pipeline.py`) cover the floor rejecting/admitting by tier, an unrecognized-resolution candidate always failing regardless of floor, ranking preferring the higher resolution when both qualify, and the pipeline actually adding a 1080p candidate once the floor is lowered and nothing higher exists — 61 total tests passing. Validated read-only against real data rather than triggering another live add: re-ran the Andrei Rublev (895) search from the earlier manual test with the floor temporarily lowered to `720p` (in-process, no `add_torrent` call) — 33 of 77 raw results qualified, correctly top-ranking a real `1080p BluRay x265 HEVC` release. Stage 7's settings list updated to name the resolution floor explicitly. Ready for Stage 3.
- **2026-09-04** — Same session, immediately after: the user asked whether seed count influences which torrent gets picked, worried a barely-seeded release would "take forever." It didn't, meaningfully — `nbSeeders` was a pure tiebreaker after composite score and file size were already equal, which real quality-tier differences almost never allow, so a well-seeded lower-tier release could easily lose to a 3-seeder higher-tier one. Fixed at two levels in `backend/app/config.py`/`score.py`: raised `MIN_SEEDERS` (the pass-two viability floor) from 1 to 10, using the user's own "only 10 seeders" example as the cutoff below which a known count is excluded outright (unknown, per Stage 0, still always passes); and added `SEEDER_TIERS` (100+/30+/10+ seeders) as a genuine — but deliberately smallest — term in the composite (`resolution*10000 + source*1000 + codec*100 + container*10 + seeder_tier*1`), so a much healthier swarm can now edge out an equally-tiered but barely-alive one without letting seed count override an actual quality difference (kept deliberately weak, matching the plan's original "never the primary key" stance and Stage 0's finding that ~50% of real rows report seeders as unknown). Unknown seeders score the same as a candidate just clearing the floor — not punished, not rewarded. Added `seeder_score` to `Score` and the CLI breakdown. 5 new unit tests, plus one updated fixture whose seed count (5) fell below the new floor — 66 total tests passing. Validated read-only against a real live Dune: Part Two search (no `add_torrent` call): 130 relevant candidates, 107 cleared the new 10-seed viability floor; also surfaced what looked at first like a scoring quirk — a release tagged `...MP4-BEN THE MEN` nudging ahead of an equally-tiered, far-better-seeded (267 vs ~10) candidate. Initially guessed this was a false-positive container match against a scene-group name and left it as-is; turned out to be wrong and worth a real fix — see the next entry.
- **2026-09-04** — Same session, continuing the real-world testing the user asked for: ran `download 693134` (Dune: Part Two) live again to get a fresh pick to cross-check by hand against the user's own qBittorrent search UI. The user's manual pick (`...REMUX-FraMeSToR`, 267 seeders, no stated container) was exactly the candidate the pipeline had ranked *second*, behind the three `MP4`-tagged ~65.8GB releases at 10-11 seeders (and one at unknown). Investigating why closed out the previous entry's "false positive" guess as actually wrong: `H265.MP4-BTM` is the full, untruncated name (the earlier read was cut off mid-groupname) — `MP4` there is a real container tag in standard `TAGS.CONTAINER-GROUP` scene convention, not a coincidental match. The real issue was two compounding weight/tiebreak problems: (1) container was weighted `×10` vs. seeder health's `×1`, so one real container point could outweigh the *entire* possible seeder-tier gap (max 2 points) — REMUX releases very often leave container unstated since MKV is the de facto default (doubly true here: TrueHD/Atmos audio barely fits in MP4 at all), so "no container token" was being scored as *worse* than a stated-but-more-questionable MP4; (2) the tiebreak order was `(composite, size_bytes, seeders_known, seeders)` — file size (a near-noise-level ~0.07% difference between postings of the same release, likely just a bundled sample file) was deciding ahead of "do we even know this swarm is alive." Fixed both in `backend/app/config.py`/`score.py`: swapped seeder health and container in the weight ladder (now `resolution*10000 + source*1000 + codec*100 + seeder*10 + container*1`, still with codec-and-above never overridable by seeder health, per the domination-margin design from the very first Stage 2 pass), and reordered the tiebreak to `(composite, seeders_known, seeders, size_bytes)`. Added 2 regression tests built directly from this exact incident (real filenames, real byte counts) — 68 total tests passing, all pre-existing tests unaffected (confirmed by hand which ones could have been, since none of them varied container independent of source/codec). Re-ran the same dry-run read-only check: FraMeSToR now ranks first (composite 45230 vs. 45211 for the `MP4` releases) — then ran `download 693134` live once more, and it added exactly `Dune.Part.Two.2024.UHD.BluRay.2160p.TrueHD.Atmos.7.1.DV.HEVC.REMUX-FraMeSToR`, matching the user's own manual pick precisely. The user had already cancelled the two earlier (pre-fix) Dune: Part Two downloads themselves before this run, so no cleanup was needed. This is the clearest end-to-end validation Stage 2 has had — a real independent human cross-check against real qBittorrent data, not just a plausibility check. Ready for Stage 3.
- **2026-09-04** — Same session: two more real `download` runs at the user's request, spanning years — `Deadpool` (2016, id 293660) and `Spider-Man: Brand New Day` (2026, id 969681, ~5-6 weeks past theatrical as of this session's date). Deadpool added an ideal pick (`...UHD.BluRay.REMUX.2160p.HEVC.HDR10.Atmos.TrueHD7.1-DreamHD`, 41GB, 33 seeders) — a well-established catalog title behaves exactly as hoped. Spider-Man surfaced a real gap instead: only 4 candidates passed pass one at all, all named generically (`4K (Small File)`/`4K (Large File)`, no source/codec/container tags whatsoever) — because no real BluRay/WEB-DL/REMUX release exists yet this soon after theatrical, so the resolution gate was passing whatever claimed "4K" with nothing to verify it against, which is exactly the profile of an early cam/upscale rip. Added a `CAM_BLOCKLIST` pass-one filter (`backend/app/config.py`/`score.py`) — `cam`, `hdcam`, `ts`, `telesync`, `screener`, `r5`, etc., always on (unlike the language lists, which default open) — that rejects an *explicitly*-tagged bootleg outright. Confirmed via unit test that `ts` (telesync) doesn't false-positive against the common `DTS`/`DTS-HD` audio tag (tokenizes as one word, never splits). Explicitly does **not** solve the Spider-Man case itself — those releases have no source tag to match against at all, so nothing currently distinguishes "genuinely unlabeled early release" from "bootleg with the label stripped." Left as a known, named gap rather than papered over — flagged to the user as such. 6 new unit tests, 74 total passing. Confirmed live (read-only) that Spider-Man's candidate set is unaffected (as predicted, none of its 4 candidates were explicitly cam-tagged) — no regression.
- **2026-09-04** — Same session, closing out: the user asked whether/how TV shows and weekly-releasing episodes (e.g. add "Lanterns" (2026) with only 3 episodes aired, auto-grab episode 4 when it airs) would work. Answer: not at all right now, and it's not a small gap — `tmdb.py` only calls `/movie/*` endpoints, `resolve.py` has no season/episode concept, and more fundamentally the entire pipeline (`download()`) is a one-shot resolve→search→score→add operation with no persistent "job" that outlives a single request. Auto-downloading future episodes needs a standing subscription, not a request — a new persisted job type that doesn't terminate, something to re-check it on a schedule, and per-episode dedup/state tracking, on top of TV-specific resolution and episode-aware search/matching. Agreed this is a real future stage (or stages) rather than a tweak, and needs its own dedicated planning session rather than being designed inline here — see the prompt handed to the user for that session below. Not added to the stage list yet; TV remains explicitly out of scope until that planning session produces a concrete design to fold in. Ready for Stage 3 (movies).

**Prompt handed to the user, for a fresh planning chat, to design TV show/episode-tracking/auto-download as new stage(s):**

> I want to plan out a new capability for the Family Downloader project: TV show discovery, episode-level resolution, and automatic downloading of newly-released episodes for shows the family is already watching — e.g. add "Lanterns" (2026) when only 3 episodes have aired, and have episode 4 auto-download the day it's available, without anyone re-opening the app.
>
> Read the full project plan first: `project.md` at the repo root — it's the persistent source of truth across every stage's chat, architecture decisions and design principles included. The movie pipeline (Stages 0-2) is built and working; TV was explicitly scoped out as "movies-only" when Stage 2 closed, and needs to be designed in properly now, not bolted on.
>
> Design this as one or more new stages, matching the plan's existing format (Purpose / Deliverables / Settled / Open decisions / Validation) and inserted at the right point in the sequence. At minimum, work through:
> - TV discovery & episode resolution (TMDB's `/tv` endpoints, season/episode metadata) — the Stage 1 equivalent for shows.
> - Episode-aware search & matching — season-pack vs. per-episode queries, `S01E03`-style token matching in pass one, reusing the existing scorer where it still applies.
> - The core architectural gap: everything built so far is a one-shot request (resolve → search → score → add → done). A "watch this show, grab new episodes automatically" feature needs a standing subscription that outlives a single request — persisted job state that doesn't terminate, plus something that re-checks it on a schedule (how often? per-show, or global?).
> - Per-episode dedup/state tracking, so a re-check doesn't re-search or re-add episodes already grabbed.
> - What the frontend needs to expose this (an "Add show" + ongoing "Watching" status, distinct from movies' one-shot "Add to Plex").
> - Whether/how this changes any "confirmed architecture" decisions already locked in (single `asyncio.Lock` request queue, SQLite persistence, no distributed job queue) now that background/scheduled work exists, not just synchronous requests.
>
> Append the outcome to `project.md`'s stage list and decision log the same way every other stage session has.

- **2026-09-04** — Stage 3 built and verified. New `backend/app/`: `db.py` (`RequestStore`, plain `sqlite3`, `requests` + `settings` tables), `worker.py` (`Worker` — queue-drain loop under an `asyncio.Lock` guarding the pipeline phase only, plus an independent download-progress watcher loop), `api.py` (FastAPI, no CORS — `POST /api/search`, `POST /api/requests`, `GET /api/requests[/…]`, stubbed `POST /api/admin/deploy`). Both open decisions closed — plain polling kept as the default, and the boot-recovery sweep reopened and resolved *differently* from the plan's draft text: only `searching` rows get force-failed on restart, not `downloading` ones (a real in-progress download shouldn't be treated as lost) — see the Stage 3 section above for the reasoning. One deviation beyond the plan's literal deliverables: seven statuses tracked, not six, since Stage 2's `"insufficient free space"` needed its own bucket rather than folding into `failed`. `pipeline.py`/`qbt.py` extended with torrent-hash capture (diff-based, works for magnet and direct-URL winners alike) so the download watcher knows what to poll. 34 new unit tests, 103 total passing. Live end-to-end validation against the real NAS through the actual API (not just the pipeline library) used a disposable title (Big Buck Bunny) chosen specifically so it wouldn't collide with anything the household actually wants: confirmed a `searching` request gets force-failed across a real backend restart, confirmed a `downloading` request (a real torrent, genuinely added) survives a real backend restart untouched, and — in the course of that — found and fixed a real bug: `torrent_hash` capture checked `existing_torrent_hashes()` only once immediately after `add_torrent()`, which is too early for a direct-.torrent-URL result (qBittorrent hadn't finished fetching/parsing it yet); fixed with a short retry loop (`HASH_CAPTURE_ATTEMPTS`/`_INTERVAL_SECONDS`), proven by a new unit test since the real indexing delay isn't deterministic enough to re-test live. Test-only config edit (`MIN_SIZE_GB` temporarily lowered to admit the disposable title's small files) was reverted immediately after use — confirmed complete because leaving it in place would have (and briefly did) broken `test_score.py`'s existing size-floor test. Disposable test torrent deleted from qBittorrent afterward; Dune from Stage 2 left untouched. Ready for Stage 4.
- **2026-09-04** — Stage 4 built and verified (with a validation caveat). Added the frontend the plan called for — one static `frontend/index.html` (hash-routed: home, provider browse, search, movie detail, requests list), `manifest.json`, generated placeholder icons, `nginx.conf`, and Dockerfiles for both `frontend/` and `backend/`, plus a root `docker-compose.yml` — see the Stage 4 section above for the full breakdown. Closed the stage's one open decision (`Cache-Control: no-cache` on the HTML, resolved via `nginx.conf`). Found and fixed a real gap while building: the plan's Stage 3 API contract never grew a `/api/discover` route, so the home grid and provider rows had nothing to call — added `GET /api/discover/{popular,trending,providers,providers/{id}}` and `GET /api/movies/{id}` to `backend/app/api.py` (thin `tmdb.py` pass-throughs, same key-never-reaches-the-browser rule as `/api/search`), with 6 new tests (109 total passing). Also caught that Stage 4's own purpose text still says "six status states" — stale since Stage 3 split out `"insufficient free space"` as a seventh; the frontend implements all seven with distinct pill colors, the plan text is just outdated now. Validated live against the real NAS (same TMDB/qBittorrent instances every prior stage used), through a throwaway same-origin proxy script standing in for nginx (neither `docker` nor `nginx` exist on this machine — a real Compose build is unverified, not just untested): home grid + pagination, provider browse + pagination, search, movie detail (via the new route), and a full real "Add to Plex" round trip using Andrei Rublev (895 — Stage 2/3's own known-no-qualifying-match title, chosen so the live run couldn't trigger a real download) that progressed queued → searching → "No matches found" in the UI exactly as the real pipeline reported it, with no torrent detail ever rendered anywhere in the DOM. Real iPad/Safari standalone-mode testing and an actual `docker compose up` are left for Stage 5 (TrueNAS has both) or a follow-up session with Docker available — flagged explicitly rather than claimed as done. Ready for Stage 5.
- **2026-09-05** — Stage 5 built and verified directly against the real TrueNAS SCALE box (SSH access granted this session — dedicated key, plus the `truenas_admin` sudo password for one-time filesystem setup). Both open decisions closed (monorepo confirmed as the existing shape; UID/GID handling confirmed identical to Docker Desktop — see the Stage 5 section above). New `truenas/custom-app-compose.yaml`, applied live via TrueNAS's `app.create`/`app.update` middleware (the same mechanism the household's own qBittorrent Custom App already uses) as an app named `movie-downloader`, frontend published on host port `8095`. Found and fixed a real bug during validation: per-file bind mounts for the frontend's static assets silently stop updating after any rename-based file replacement (which is exactly how `sed -i` and `git`'s own checkout write files) — Docker pins a file-level bind mount to the source inode, so this would have quietly broken "frontend picks it up on next request" the first time Stage 6's `git pull` deploy touched `index.html`. Fixed by switching to one directory-level mount for all of `frontend/`, which required a new `nginx.conf` `deny`/404 rule so that same directory's `Dockerfile`/`nginx.conf` don't become servable static files — both fixes verified live (edited `index.html` directly on the deployed-copy mount and confirmed the change served with no rebuild/restart; confirmed `nginx.conf`/`Dockerfile` both 404 while the real homepage and `/api/*` proxy still 200). Also discovered along the way that `app.update`/`app.redeploy` don't rebuild a `build:`-context custom app's image on their own — a manual `docker compose build --no-cache` was needed — which extends Stage 6's existing "`requirements.txt` needs a manual image recreate" open decision to cover `frontend/Dockerfile`/`nginx.conf` and `backend/Dockerfile` too; ordinary `backend/app/` and `frontend/` content changes don't have this problem, both confirmed live-reloading correctly. Validated end-to-end against the real deployed stack (not a proxy stand-in, unlike Stage 4's necessarily more limited pass): reachable from another LAN machine (no physical iPad available this session either), a full real `POST /api/requests`/poll round trip against the real qBittorrent instance through the real deployed containers using Andrei Rublev (895, the same disposable known-no-match title used for exactly this purpose since Stage 2), and a full `app.stop`/`app.start` cycle confirming the SQLite job store survives a real restart on real TrueNAS storage. `backend` reaches qBittorrent via the NAS's own LAN IP (`192.168.0.133:30024`, unchanged from every prior stage) since qBittorrent is a separate Custom App with its own compose network — true container-to-container DNS isn't available across separate apps in this app-store model, but nothing about that changed from what every prior stage already relied on. Nothing in this stage's session was committed or pushed to git yet — left for the user to decide. Ready for Stage 6.
- **2026-09-05** — Stage 6 built end-to-end, including live NAS/GitHub rollout. Session started with no SSH access and no `gh` auth (confirmed directly: `ssh 192.168.0.133` → permission denied, `gh auth status` → not installed) — unlike prior stages, access had to be requested mid-session rather than being available up front. User granted a dedicated SSH key (`~/.ssh/truenas_movie_downloader`, already pre-authorized on the NAS as `truenas_admin`) plus the sudo password, same pattern as Stage 5. Code written and locally validated first (see the Stage 6 section above for the full breakdown: `app/deploy.py`'s `run_git_pull()`, the real `POST /api/admin/deploy`, `backend/Dockerfile`'s `git`/`openssh-client`/`safe.directory` additions, `truenas/custom-app-compose.yaml`'s new mounts, 183 backend tests passing including 4 new ones against real offline git repos). Mid-session, the user asked for the gating control to change from the originally-planned hidden long-press to a plain visible "Check for updates" button — simpler, same accepted-risk reasoning — implemented and re-verified before touching the NAS.

  Live rollout: generated the deploy keypair directly on the NAS (private key never left the box), user added the public half to GitHub's Deploy Keys, switched the deployed clone's `origin` to SSH and confirmed a real authenticated `git fetch`/`pull`. While reconciling the deployed clone before syncing it, found it was 2 commits behind `origin/main` with an *uncommitted* local edit to `frontend/nginx.conf` — Stage 5's own live nginx fix, applied by hand on the NAS during that session but never committed, exactly as that stage's decision-log entry had flagged as still-open. Confirmed the uncommitted content was byte-identical to what the missing commits already contained before touching anything, then `git stash` + `git pull --ff-only` safely caught the clone up with zero risk. Applied the updated compose config live via `midclt call app.update` (the Claude Code permission classifier initially blocked this as a shared-infrastructure change; re-attempted after the user explicitly approved it, then succeeded) and rebuilt the backend image with `docker compose build --no-cache` — required for the Dockerfile's new `git`/`openssh-client` line to actually take effect, confirming this stage's own "manual rebuild" open decision the hard way (first rebuild attempt ran against the *old* Dockerfile because the code hadn't been pushed to GitHub yet at that point — caught immediately, fixed by committing and pushing before rebuilding again). The redundant stash entry from the reconciliation above was dropped cleanly once confirmed empty/no-op. Final validation: pushed the real commit, pulled it onto the NAS with the real deploy key, rebuilt with the real Dockerfile, then proved the whole loop live in a real browser against `http://192.168.0.133:8095/` — a real "Check for updates" click ran a real `git pull` and reported `48aad57`, and a real `git checkout <sha>`/`git checkout HEAD` round trip on the deployed-copy mount proved rollback (and roll-forward) both take effect on the next page load, no rebuild or restart needed. NAS repo left clean (`git status` empty, no stashes, `origin` on SSH, at `48aad57`). Ready for Stage 7.
- **2026-09-05** — Stage 7 built and verified (see the Stage 7 section above for the full breakdown: `backend/app/pipeline_settings.py`'s `PipelineSettings` dataclass, the `settings: PipelineSettings | None = None` parameter threaded through `score.py`/`pipeline.py` with a config.py-derived default so every pre-existing test kept passing unmodified, `worker.py` resolving real settings from the store per request, the new `GET`/`PUT /api/settings/pipeline` routes, and the frontend's new "Download preferences" section). No SSH/NAS access needed or used this session — everything validated locally against the real TMDB API and the real NAS-hosted qBittorrent instance (both reachable directly from this workstation over the LAN), through the same kind of throwaway same-origin FastAPI+StaticFiles stand-in Stage 4/5 used in place of nginx (still no docker/nginx on this machine). 183 pre-existing backend tests passed completely unchanged after the settings-threading refactor — confirming the "default settings computed fresh from config.py, not captured at import time" design actually delivers backward compatibility rather than just being a nice idea. 23 new tests added (206 total): `pipeline_settings.py`'s own resolve/overlay/validation logic, explicit-`PipelineSettings` variants of the score/pipeline tests (the actual runtime path, as opposed to monkeypatching `config.py`), and the new API routes including validation.

  Scoped the plan text's "Stage 2's quality-tier weights/defaults" down to just the resolution floor (the one real per-household quality preference this pipeline has) rather than exposing the underlying resolution/source/codec/seeder/container point values themselves — those were reworked three times against live data during Stage 2 specifically to keep a strict domination order between tiers, and a casual edit through a settings field could silently break that guarantee with no error to show for it. Flagged as a scoping call rather than assumed to be obviously correct.

  Found a real validation gap by actually trying to break the new endpoint rather than just trusting the pairwise check: an isolated `PUT {"min_size_gb": 500}` (nothing said about `max_size_gb` in that same request) passed the "min < max" validator as originally written, then silently produced an unsatisfiable 500–150 GB effective range once `max_size_gb` fell back to its 150 GB config default on the next read — not a crash (an inverted range just means the viability gate rejects everything, which degrades to "no qualifying results" exactly as the plan's fail-safe principle asks), but confusing and clearly not what a Settings-panel edit should silently do. Fixed by validating the *prospective merged* state (current settings-table row + this request's patch, via the new `settings_from_raw()` helper) rather than just the two fields when both happen to appear in the same PUT; added a regression test built directly from this incident, then reset the local dev-only settings row (not the NAS's) back to defaults once confirmed. Proved the settings-to-pipeline wiring end-to-end without ever calling `add_torrent`: Big Buck Bunny (tmdb_id 10378, Stage 3's own disposable public-domain test title) went from 0 candidates clearing the viability gate at the default `min_size_gb=1` to 2 clearing it once lowered to `0.1` through a real `PipelineSettings` object built the same way `worker.py` builds one from the store, against real live qBittorrent search results. Nothing in this session touched the real NAS deployment — Stage 7's code needs a `git pull` (or "Check for updates") on the deployed clone before the live app reflects it. Ready for Stage 8.
- **2026-09-05** — Stage 8 built and verified (a pass, not new scope, per the plan's own framing — see the Stage 8 section above for the full breakdown). Asked the user up front about the two genuinely optional deliverables the plan left open: push-notification-on-failure (ntfy) and automatic Plex scan-on-complete — both declined, keeping this stage to structured logging, the health endpoint, and the failed-jobs view. New `backend/app/logging_config.py` (`configure_logging()`, called once from `api.py`); `worker.py` gained a per-transition `logger.info` line wherever one wasn't already there (the pipeline's outcome with variant/score via a new `_score_summary()` helper, and both `downloading -> complete`/`downloading -> cancelled` paths in the download watcher); `api.py`'s manual cancel route got the same treatment. `qbt.py` gained `ping()` (a cheap `app_version()` call, `try/except Exception: return False`) backing the new `GET /api/health` (`{"status": "ok", "qbittorrent": bool}`, deliberately always `200` — an unreachable qBittorrent is a dependency fact, not a reason to fail the backend's own healthcheck, per "fail safe, not best guess"). `GET /api/admin/jobs?status=...` added, gated exactly like `/api/admin/deploy` (visible, no auth), defaulting to every failure-shaped status (`failed`/`no qualifying results`/`insufficient free space`, deliberately excluding `cancelled` as usually a user choice rather than a failure) and narrowable to one. 7 new unit tests, 213 total passing.

  No SSH/NAS access used or needed this session — this stage is pure observability plumbing with nothing new to cross-check against live search/scoring data, so validation stayed at the unit-test level (fake-qBittorrent unreachability for the health check, genuinely-failed/no-match/no-space rows for the admin endpoint, asserting the transition log lines actually fire and carry variant/score) rather than a live induced-failure pass against the real NAS. That real pass — killing the actual qBittorrent app mid-request and watching `/api/health` flip plus the transition log appear in TrueNAS's own app log viewer — is flagged as still worth doing once this deploys, the same way Stage 4's frontend caveat got closed out by Stage 5. Nothing in this session touched the real NAS deployment — both Stage 7 and Stage 8 need a `git pull` (or "Check for updates") on the deployed clone before the live app reflects either. This closes out every stage in the plan's original spine — no further stages are queued (the TV/episode-tracking capability flagged in the 2026-09-04 decision-log entry above remains its own separate, not-yet-designed future work, per that entry's own framing).
- **2026-09-05** — Same day, immediately after: the user pulled Stage 7+8 onto the real NAS via the Settings panel's "Check for updates" button (no SSH needed — the same visible-button flow Stage 6 built). Confirmed the deploy genuinely took effect, not just trusted the report: direct `curl`s from this workstation to the live deployed stack (`http://192.168.0.133:8095/`, LAN-reachable the same way every prior stage's validation was) returned real responses from both new Stage 8 routes — `GET /api/health` → `{"status":"ok","qbittorrent":true}`, and `GET /api/admin/jobs` → the real Andrei Rublev "no qualifying results" row left over from Stages 3-5's own disposable validation runs, returned as raw JSON exactly as designed — plus `GET /api/settings/pipeline` confirming Stage 7 is live too. This closes the "still worth doing once deployed" live-validation gap the Stage 8 write-up flagged above. The plan's original stage sequence is now fully built *and* running live.
- **2026-09-05** — TV planning session, per the prompt this file has carried since Stage 2 closed (2026-09-04 entry above). Read the whole plan plus `backend/app/{tmdb,resolve,score,pipeline,pipeline_settings,worker,db,api,config}.py` to ground the design in what's actually reusable rather than designing in the abstract. Produced Stages 9-12 above (TV discovery & episode resolution; episode-aware match/score/download; subscriptions, scheduling & per-episode dedup; TV frontend & API), reviewed with the user via a plan-mode proposal and approved as written — no pushback on any of the calls below. *(Renumbered 2026-09-06 below to 9, 10, 12, 13 — a new Stage 11 was inserted between the episode-matching and subscriptions stages; every "Stage 11"/"Stage 12" reference in this entry has been updated in place to the new numbers rather than left stale, since they describe the same content either way.)*

  Key findings that shaped the design: `worker.py` already runs three independent `asyncio.sleep`-loop background tasks (`_process_queue` under the pipeline lock, `_watch_downloads`, `_watch_retention`), so a fourth loop for show-checking (`_watch_shows`, Stage 12) is the same established pattern, not new architecture — directly answering the deferred prompt's question about whether background/scheduled work changes any locked-in architecture decision. Answer: no. The single `asyncio.Lock`, SQLite persistence, and no-distributed-queue decisions all hold unchanged; the scheduler loop only talks to TMDB and a new dedup table, then calls the same `enqueue()` movies already use. `score.py`'s pass-two scorer and `normalize.py`'s whole-token matching are release-shape-agnostic and are reused completely unchanged for episodes — only a new pass-one episode-token check (`s01e04`-style, whole-token, no year-tolerance) is genuinely new matching logic.

  Real scoping calls made this session, all flagged as such rather than silently assumed: **season packs are explicitly out of scope for v1** (Stage 10) — a season-pack add doesn't fit a per-episode dedup ledger cleanly, and a solo season-pack-shaped result is now supposed to be *rejected*, not accepted, by the new pass-one gate; **catch-up-on-add is the only behavior** (Stage 12) — adding a show always backfills every already-aired episode, directly answering the deferred prompt's own "Lanterns" worked example, with no opt-out toggle in v1; **specials excluded by default**; **only the latest season gets checked each cycle** (plus watching `number_of_seasons` for a season rollover), an accepted gap for an old season resuming after a long hiatus. One decision deliberately left open rather than settled: the show-recheck interval (proposed default 6 hours, a plain `config.py` constant, no Settings-panel control in v1) — this trades promptness against indexer request volume in a way that's a genuine household preference, not an engineering call, so it's flagged for revisiting at or before Stage 12's build rather than guessed at here.

  Data-model decision: extend the existing `requests` table with nullable `media_type`/`show_id`/`season_number`/`episode_number` columns rather than building a parallel audit-trail table for episodes — an episode's search/score/add history is a normal `requests` row and gets every existing status, the download watcher, retention purge, and cancel endpoint for free. Two new tables (`shows`, `show_episodes`) hold only the subscription and the dedup ledger, which are genuinely new concepts a movie request never needed.

  No code written this session — this was explicitly a design-only session, matching how the 2026-09-04 entry originally framed it ("needs its own dedicated planning session... Not added to the stage list yet"). Stage 9 is ready to build as a normal implementation session next.
- **2026-09-06** — Stage 9 built and verified (see the Stage 9 section above for the full breakdown: `tmdb.py`'s new `/tv` wrappers, `tv_resolve.py`'s `ShowIdentity`/`episode_query`/`resolve_show`, and `generate_variants`'s move from `resolve.py` into `normalize.py` so it's genuinely shared rather than duplicated for shows). No SSH/NAS access needed or used this session — validated locally against the real TMDB API (reachable directly from this workstation, same as every prior TMDB-touching stage) plus the local unit test suite; nothing about this stage touches qBittorrent or the deployed NAS stack. 227 tests passing (213 pre-existing + 14 new), all pre-existing tests — including `test_resolve.py`'s `generate_variants` cases — unchanged and still green after the extraction into `normalize.py`, confirming the refactor is genuinely behavior-preserving rather than just plausible.

  Live TMDB validation happened to land on a real instance of the plan's own "Lanterns" worked example from the 2026-09-04 TV-planning entry: `search_tv("Lanterns", year=2026)` found the real show (tmdb_id 95350); `resolve-show 95350` correctly resolved it with one variant (no subtitle/original-title/year to add); `get_tv_season(95350, 1)` returned 8 real episodes, 4 already aired as of today (2026-09-06, through "The Weenie") — precisely the "some episodes aired, grab the next one" shape Stage 12's catch-up-on-add design is built around, confirmed against real data rather than a hypothetical. Also confirmed live: `get_tv_popular`/`get_tv_trending` return real TV rows and are genuinely TTL-cached (same mechanism Stage 1 built, no new caching code), and `resolve-show 1399` (Game of Thrones) round-trips cleanly as a no-subtitle, single-variant case. The subtitle-splitting and original-title-differs paths (`Star Trek: Discovery`, Attack on Titan) were exercised through `test_tv_resolve.py`'s fixture-based unit tests rather than a second live call, since both reuse `normalize.py`'s already-live-tested (Stage 1) logic verbatim — nothing new to prove there beyond "the shared function still gets called correctly for a show."

  Nothing committed or pushed this session, and nothing deployed to the NAS — left for the user to decide, same as Stage 5's precedent. Ready for Stage 10.
- **2026-09-06** — Same day, plan-only follow-up (no code): the user flagged a real gap in the Stages 10-12-as-designed sequence — once episodes start downloading one at a time under different release names, nothing in the existing design ever gets them into a shape Plex's TV agent recognizes as "this show, this season, this episode." A movie's one-shot download didn't need this (a single file per request, and Plex's movie matching tolerates a looser folder layout); a show accumulating individually-named episode files across a season is a materially different problem, and the plan as it stood genuinely didn't address it — worth fixing before Stage 10/11(old) got built, not after.

  Inserted a new **Stage 11 — Post-download file organization & Plex library layout** between episode-matching and subscriptions, renumbering the two stages after it (old Stage 11 "Subscriptions, scheduling & per-episode dedup" → Stage 12; old Stage 12 "TV frontend & API surface" → Stage 13) — every cross-reference elsewhere in this file (Stage 10's season-pack note, the 2026-09-05 entry above, the Status line) updated to match rather than left pointing at stale numbers. Core design decision: since this pipeline already positively resolves show/season/episode identity *before* searching (Stage 9/10), file placement can be a deterministic write to a `{tmdb-<id>}`-hinted path rather than a filename-guessing heuristic — which is what actually answers the user's "have the correct show detected" concern, more directly than leaning on Plex's own matching ever could. Also corrected the user's own proposed mechanism on a technical point, flagged explicitly rather than silently implemented differently: renaming a file *while it's still downloading* risks qBittorrent's resume data for no benefit; a hardlink placed once the torrent is genuinely complete (the established Sonarr/Radarr pattern) gets the same practical result — Plex sees a correctly-named, correctly-placed file — without ever touching the live download.

  Flagged, not yet resolved, as this session's most consequential open thread: this introduces a **new infra dependency** — the backend container needs a bind mount into the same filesystem/dataset qBittorrent downloads into and Plex's TV library already lives on, so a hardlink is even possible. That's a real NAS-layout question (exact host paths, whether qBittorrent's current save location already qualifies) that needs the real box, not a planning session — left as an explicit open decision in the new Stage 11 rather than guessed at. Two smaller open decisions also left for build time: episode-title-in-filename vs. `SxxEyy`-only, and whether a cross-filesystem hardlink failure should silently fall back to a copy or hard-fail into the new `"downloaded, not filed"` status. No code written this session. Ready for Stage 10.
- **2026-09-06** — Same day, second plan-only follow-up (no code): the user raised a second real gap — Stage 12's per-episode standing subscription is the wrong tool for a show that's been over for years. Auto-grabbing 100+ episodes one search at a time is slow, hammers the indexer, and old catalog TV frequently has no well-seeded single-episode releases at all, only season or complete-series packs — which is exactly the shape Stage 10 was built to *reject*, on purpose, to keep the per-episode dedup ledger clean.

  Inserted a new **Stage 13 — Whole-season & complete-series bulk acquisition** between subscriptions (Stage 12) and the TV frontend, renumbering the old Stage 13 ("TV frontend & API surface") to Stage 14; the Status line above updated to match. Deliberately framed as a second, independent acquisition mode rather than a reopening of Stage 10's settled call — Stage 10's episode-search pass-one gate still rejects a pack-shaped result exactly as before (protecting the per-episode auto-grab loop), while the new stage adds its own opposite-shaped gate that specifically *wants* a season-or-more result, used only behind an explicit user-triggered `POST /api/shows/{id}/bulk-download`, never automatically. The two modes are explicitly independent, not mutually exclusive — bulk-grabbing seasons 1-5 of a still-airing show while staying subscribed for new episodes is a real, supported combination, not just the ended-show case that prompted this.

  Two deliberate reuse decisions worth flagging: (1) `media_organizer.py`'s Stage 11 per-episode placement function is called once per file inside a pack rather than writing a parallel implementation — a pack is just "many files, each individually SxxEyy-tagged," so the existing per-file logic already does the real work; (2) one pack torrent add fans out into **one `requests` row per episode** (all sharing the pack's `torrent_hash`) rather than a single opaque "pack" row, so the entire existing requests-list/retention/admin-visibility/status machinery keeps working with zero changes — the same reuse-not-duplicate call Stage 12 already made for scheduled per-episode adds, applied again here. Every organized episode also gets backfilled into Stage 12's `show_episodes` ledger, so a show bulk-downloaded today and subscribed-to tomorrow doesn't trigger a redundant re-search.

  Scoped deliberately narrow for v1, flagged rather than silently assumed: only two pack shapes are recognized (one full season, or an explicit complete-series marker) — a partial multi-season range (`S01-S03` of an 8-season show) is a named, documented gap, matching the project's running style of naming a gap instead of half-solving it. Left open for the build session: what to do when a "complete series" pack turns out to be missing a season TMDB knows has aired (accept the partial result vs. reject and retry another variant — leaning toward accepting it, per "fail safe, not best guess," but not settled here), and whether the new pass-one logic is worth its own `pack_score.py` file or belongs directly in `score.py`. No code written this session. Ready for Stage 10.

