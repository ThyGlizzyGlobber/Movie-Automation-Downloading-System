requests>=2.31
python-dotenv>=1.0
pytest>=8.0
qbittorrent-api>=2024.1
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
yt-dlp>=2024.1
# Frontend migration Part G4 — rate limiting on /api/auth/* and
# /api/setup/*, the routes an internet attacker would actually script
# against once remote access is enabled.
slowapi>=0.1.9
# Web Push for request notifications (Settings › Notifications).
pywebpush>=2.0
